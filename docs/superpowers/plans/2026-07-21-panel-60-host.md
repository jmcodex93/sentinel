# Fase 6.0 — Host dockeable + shell + Overview — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Register the new dockable SPA panel (`Sentinel Panel (SPA)`) with the adaptive rail + header + "shot health" dashboard (4 read-only cards with quick actions), living alongside the untouched native panel. Delivers the 6.0 dock spike.

**Architecture:** Spec madre `docs/superpowers/specs/2026-07-21-panel-spa-design.md` (sección 6.0). New `PanelSPADialog` (full-bleed HTMLVIEWER `?page=panel`, Timer = drain + pump_jobs, GC retention) + `SentinelPanelSPACmd` (Execute/RestoreLayout mirror of `YSPanelCmd`, panel.py:2877-2893 — that is what makes it dockable + layout-persistent). New thin `panel_ops.py` (state stamp reusing hub's `_stamp_for`, overview payload over existing engines). SPA `PanelPage` with ResizeObserver rail breakpoint. Refresh = stamp polling (spike verdict: PostWebMessage descartado).

**Tech Stack:** the established stack; no new deps.

## Global Constraints

- Branch `feat/panel-spa` off main. Baselines: pytest 667, vitest 51. Trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Native panel untouched (parallel-panel strategy). New plugin ID: next free in the 2099069+ range — grep `2099` across the repo; expected free: 2099076 (2099074 must be verified too; document the choice in `common/constants.py` beside the existing ID comment).
- Ops: thin adapters only; QC data comes from the SAME shared scoring helpers as Reports/preflight (`run_all_checks`/`compute_score` fase-2 call shapes — copy from `_op_report_qc`); overview must NOT trigger re-checks on poll — polling reads the stamp; the SPA fetches `panel/overview` only when the stamp changes.
- Quick actions reuse EXISTING ops: fixes via `palette/run` (ids fix_lights/fix_cameras/fix_materials/fix_fps + confirm contract), navigation via `palette/run` (open_hub, open_reports_qc, open_reports_delivery...), Save Version/Notes open the existing `form/*` windows for now (absorption = fase 6.3).
- Design: DESIGN.md tokens; rail badges use status chroma (fail/warn) — they ARE state; breakpoint ≥560px = labeled sidebar, else icon rail; `lucide-react` icons.
- Docs claim no live verification until the live task.

## File Structure (locked)

```
plugin/sentinel/ui/panel_ops.py       # NEW: panel/state_stamp, panel/overview (+PANEL_OPS)
plugin/sentinel/ui/panel_spa.py       # NEW: PanelSPADialog + SentinelPanelSPACmd + open helper
plugin/sentinel/ui/reports_dialog.py  # merge **PANEL_OPS
plugin/sentinel/common/constants.py   # SENTINEL_PANEL_SPA_PLUGIN_ID
plugin/sentinel_panel.pyp             # register the new command
tests/test_panel_ops.py               # NEW (fake-c4d harness)
web/src/pages/PanelPage.tsx           # NEW
web/src/components/panel/{PanelRail,PanelHeader,OverviewCards}.tsx  # NEW
web/src/lib/panel.ts (+ .test.ts)     # pure card-shaping/breakpoint logic if non-trivial
web/src/mock/panel-overview.json      # NEW
web/src/types.ts / web/src/lib/api.ts # panel types + fetchers
web/src/App.tsx                       # "panel" page branch (full-bleed)
```

---

### Task 1: `panel_ops.py` — state stamp + overview

**Interfaces:**
- `"panel/state_stamp"` — reuse hub's `_stamp_for(doc)` (import from `hub_ops`) → `{"stamp"}`; doc guard.
- `"panel/overview"` (read-only) → payload:
```
{ "scene": {"name", "path_set": bool, "shot_id", "artist", "version_label", "version_age",
             "polys_label"?},           # from doc + versioning history (reuse the helpers
                                        # form/save_version/state uses — copy call shapes)
  "qc": {"passed", "total", "disabled", "top": [{"check_id","label","count"} ≤3 worst],
          "fixable": [palette action ids currently enabled]},   # SAME scoring as _op_report_qc
  "assets": {"count", "missing", "disk_label", "vram_label"},   # scan_scene_assets +
                                        # compute_totals + cached vram (hub helpers)
  "render": {"preset_name", "fps", "resolution", "multiformat": bool},
  "deliver": {"todos_pending", "notes_present", "last_delivery_age"?} }
```
  Field sources MUST be copied from the existing call sites (versioning/notes helpers in `flows.py`/`web_ops.py`; render data via `doc.GetActiveRenderData()`). Anything unavailable → null, never invented. `PANEL_OPS` dict merged in reports_dialog.
- Tests: registration, no_document contracts, plus pure payload shapers extracted where feasible.
- [ ] TDD → implement → focused + full pytest → commit `feat(panel): panel_ops — state stamp + overview sobre motores existentes`.

### Task 2: Host — `panel_spa.py` + registration

**Interfaces:**
- `PanelSPADialog(gui.GeDialog)`: CreateLayout = single `CUSTOMGUI_HTMLVIEWER` (`?page=panel`), browser fallback like ReportsDialog; `InitValues` SetUrl; `Timer(25)` = `_queue.drain(_dispatch)` + `pump_jobs()` (guarded); `DestroyWindow` pass. Server via `ensure_server()`.
- `SentinelPanelSPACmd(plugins.CommandData)`: mirror `YSPanelCmd` EXACTLY (panel.py:2877-2893): held `dlg`, `Execute` → `dlg.Open(dlgtype=c4d.DLG_TYPE_ASYNC, pluginid=SENTINEL_PANEL_SPA_PLUGIN_ID, defaultw=420, defaulth=640)`, `RestoreLayout` → `dlg.Restore(pluginid=..., secret=sec_ref)` — this is what enables docking + layout persistence.
- Constants: `SENTINEL_PANEL_SPA_PLUGIN_ID` (verified free) in `common/constants.py`; register in `sentinel_panel.pyp` as `"Sentinel Panel (SPA)"` with the same icon loader.
- Tests: import-safety under the fake harness (module imports without c4d explosion); ID uniqueness test (grep-style assert vs known IDs).
- [ ] Implement → pytest full → commit `feat(panel): host dockeable SentinelPanelSPA (Execute/RestoreLayout, Timer drain+pump)`.

### Task 3: SPA — PanelPage (rail + header + Overview)

**Interfaces:**
- `App.tsx`: `"panel"` full-bleed branch (like hub).
- `PanelRail`: ResizeObserver on the page root; `<560px` → 44px icon rail (lucide: House, ClipboardCheck, Clapperboard, Package, Wrench + CommandIcon bottom), `≥560px` → 132px labeled sidebar. Badges: QC fails (fail bg) and missing (warn bg) fed from overview. Sections beyond Overview render a "próximamente — usa el panel nativo" placeholder linking the native equivalents (6.1-6.4 fill them).
- `PanelHeader`: scene name (title), version pill + age, QC score bar (shared visual language with the mock approved in brainstorm).
- `OverviewCards`: 4 cards per the approved mockup; quick-fix buttons via `runPaletteAction` (confirm contract copied from PalettePage/HubPreflightStrip), deep-links via palette navigate/run ids; Save Version/Notes buttons → `runPaletteAction("save_version"/"edit_notes")` → those return `{navigate}` for FormApp… NO: in panel context, opening the form WINDOW is done natively — use the palette RUN semantics: check how `palette/run` handles navigate-kind actions when invoked from a non-palette page (it returns `{ok, navigate: page}`) — the panel should instead call a tiny new op? SIMPLEST correct: reuse `_palette_open_reports`-style native window opens — `save_version`/`edit_notes` palette entries are kind "navigate" (SPA-internal). For 6.0, have the two version-card buttons call a NEW thin op `panel/open_form {page}` that calls `open_form(doc, page)` natively (mirror `_palette_open_hub`), added to `panel_ops.py` with a registration test. Keep it to exactly that.
- Polling: stamp every 2s while visible (mock-gated), re-fetch overview on change; mutations via palette (fixes) re-fetch after success.
- Mock `panel-overview.json` + `?mock=1` flow.
- [ ] Implement (+ vitest for any pure shaping) → vitest + build clean (restore `plugin/web/`) → commit `feat(web): PanelPage — rail adaptativo, header, dashboard salud del shot`.

### Task 4: Build + docs

- [ ] `npm ci && vitest && build` — COMMIT `plugin/web/`; full pytest (record).
- [ ] CLAUDE.md: new `**v1.19.0**` entry (Fase 6.0, live pendiente); PLUGIN_VERSION bump; ROADMAP: 6.0 done + 6.1-6.4 listed; spec madre: anotar resultado pendiente del spike de dock.
- [ ] Commit `feat(panel): fase 6.0 — host + shell + overview (build + docs)`.

### Task 5: Live (controller MCP + user) — el spike de dock

- Restart C4D → command "Sentinel Panel (SPA)" opens the panel; **user docks it** next to the viewport.
- Checklist: webview renders docked; resize across the 560px breakpoint (rail ↔ sidebar); Cmd+Z passthrough while docked; polling refresh (edit scene → cards update); quick fix roundtrip (fix_lights + confirm-gated fix_materials); deep-links open Hub/Reports; Save Version button opens the form window; **layout persistence**: restart C4D with the panel docked in the saved layout → RestoreLayout revives it with content (the classic GC/blank-window trap — verify against it).
- Document the spike verdict in CLAUDE.md (capability or limitation), fix waves as needed; then final increment review + merge/push v1.19.0 with user go-ahead.

## Self-Review (plan time)

- Spec-madre 6.0 coverage: host+registro ✅T2, rail+header+dashboard ✅T3, ops ✅T1, spike dock ✅T5, paralelo sin tocar nativo ✅ (solo .pyp añade registro). Quick actions reutilizan palette; única op nueva de apertura `panel/open_form` justificada y mínima.
- Consistency: stamp reutiliza `_stamp_for` del hub; QC = helpers fase 2; payload fields con fuentes nombradas; plugin ID verificado libre antes de usar.
