# Sentinel Plugin - Development Rules

## Project Overview
Sentinel (v1.22.0) is a Cinema 4D quality control and workflow automation plugin designed for professional 3D production workflows. **Originally built as YS Guardian at Yambo Studio**, now maintained and extended by Javier Melgar as Sentinel — keeping the watchdog spirit while expanding into versioning, status tracking, and modern mograph workflow tools. It acts as a real-time watchdog that continuously monitors scenes for production issues, plus provides render management and scene tools.

The plugin performs **12 quality checks** in real-time:
1. **Lights Organization** - Ensures all lights are properly organized in a "lights" group (Select + Fix)
2. **Visibility Consistency** - Detects objects with mismatched viewport/render visibility (Select)
3. **Keyframe Sanity** - Warns about multi-axis keyframes that can cause animation issues (Select)
4. **Camera Shift Detection** - Alerts when cameras have non-zero shift values (Select + Fix)
5. **Render Preset Compliance** - Ensures only approved render presets exist (Info)
6. **Assets (Textures)** - Missing textures, absolute paths, RS Node material paths (Info)
7. **Unused Materials** - Detects materials not applied to any object (Select + Fix)
8. **Default Names** - Objects still using C4D default names like "Cube", "Null" (Select)
9. **Output Paths** - Missing tokens, empty paths in render settings (Info)
10. **Take Validation** - Camera assigned per take, output paths with $take token (Info)
11. **FPS / Frame Range** - Validates FPS, start frame = 1001 (VFX standard), frame step, timeline + preview alignment, all presets (Info + Fix)
12. **Cross-Aspect Safe Area** - Verifies opt-in marked subjects (UserData) stay inside per-format safe-area regions when delivering across multiple aspect ratios via Multi-Format Setup. Auto-refresh uses current frame; Info button runs full keyframe sweep (Select + Info)

Additional features: RS AOV management (Essentials/Production/Light Groups), Scene Collector, QC Report export, Render Presets with aspect ratio toggle, Texture Repathing tool (multi-renderer bulk find/replace + smart-fix), and a full suite of scene tools.

## Core Files (DO NOT DELETE)
- `plugin/sentinel_panel.pyp` - Cinema 4D bootstrap only: inserts the plugin root on `sys.path`, imports the `sentinel/` package, keeps all `Register*` calls, and preserves the `__main__` guard.
- `plugin/sentinel/` - Sentinel Python package. Current layout:
  - `common/` - settings, cache, constants, shared helpers
  - `checks/` and `qc/` - QC check implementations, registry, result adapters, scoring
  - `aovs.py`, `baseline.py`, `fixes.py`, `multiformat.py`, `notes.py`, `rules.py`, `safe_areas.py`, `snapshots.py`, `textures.py`, `versioning.py` - extracted workflow engines (`fixes.py` = QC auto-fix machinery + batched `apply_fixes`; `snapshots.py` = pure EXR→PNG helpers, no gui)
  - `ui/ids.py`, `ui/user_areas.py`, `ui/dialogs.py`, `ui/frame_tag.py`, `ui/reports.py`, `ui/flows.py`, `ui/scene_tools.py`, `ui/panel.py` - widget IDs, custom UserAreas, dialogs, Sentinel Frame tag, QC-report assembly, save/collect/gate/snapshot flows, scene-tool mutations, and the main GeDialog panel (thin UI layer, ~2200 lines after the v1.10 extraction; business logic lives in the engine + `ui/{reports,flows,scene_tools}`)
- `plugin/res/` - Resource descriptions required by C4D for plugins that need a `description` parameter (e.g. SafeAreaOverlayObject in v1.5.6). Contains `c4d_symbols.h`, `description/safearea_overlay.res|.h`, `strings_us/description/safearea_overlay.str`. Adding new ObjectData/TagData plugins needs new `.res|.h|.str` triplets here.
- `plugin/exr_converter_external.py` - Cross-platform EXR→PNG with ACES pipeline
- `plugin/abc_retime/` - Bundled ABC Retime plugin (by axisfx2)
- `plugin/legacy/` - Archived snapshot files (kept for reference)

## Development Flow
- After changing package structure or classes registered with Cinema 4D, restart Cinema 4D. Do not rely on "Reload Python Plugins" for package reloads: live ObjectData/GeDialog instances can keep references to old module objects and create split-brain state.
- Install/copy the full plugin folder contents together: `sentinel_panel.pyp`, `sentinel/`, `res/`, `abc_retime/`, and support scripts. The `.pyp` alone is not a complete plugin anymore.

## Development Rules

### 1. FOCUS
- **ONE PROBLEM AT A TIME**: Don't try to solve everything at once
- **CORE FUNCTIONALITY FIRST**: Get the basic feature working before adding complexity
- **NO FEATURE CREEP**: Don't add features that weren't requested

### 2. FILE MANAGEMENT
- **EDIT, DON'T CREATE**: Modify existing files instead of creating new versions
- **NO HELPER SCRIPTS**: Don't create installation scripts, test scripts, or diagnostic tools unless specifically requested
- **KEEP IT SIMPLE**: The fewer files, the better

### 3. PROBLEM SOLVING
- **IDENTIFY ROOT CAUSE**: Understand WHY something isn't working before trying to fix it
- **TEST INCREMENTALLY**: Make small changes and test each one
- **DOCUMENT FINDINGS**: Keep notes about what works and what doesn't in this file

### 4. CODE PRINCIPLES
- **MINIMAL DEPENDENCIES**: Use only Cinema 4D's built-in Python libraries when possible
- **FALLBACK GRACEFULLY**: If a feature can't work, fail silently with a simple message
- **NO OVER-ENGINEERING**: Simple solutions are better than complex ones

### 5. GOAL-DRIVEN EXECUTION
Before coding, restate the task as a verifiable success criterion so the loop can close on its own instead of waiting for the user to eyeball it.
- **"Add QC check X"** → "Open a scene that violates X, run QC, confirm the new check reports it; open a clean scene, confirm it doesn't."
- **"Fix bug Y"** → "Reproduce Y in C4D first, then verify the fix makes the repro pass."
- **"Refactor Z"** → "All 12 QC checks still produce the same results before and after."
- **"Add UI button"** → "Plugin reloads without errors, button appears in expected section, click triggers the action, status text updates."

For multi-step work, write the plan as `step → verify` pairs. Weak criteria ("make it work") force ping-pong; strong criteria let the work finish in one pass.

## Data Persistence

### Saved Per Computer/User (Persistent via `sentinel_settings.json`; legacy `ys_guardian_settings.json` is auto-migrated on first run)
- **Artist Name**: Stored in Cinema 4D preferences folder
- **Compositor Target**: Nuke vs After Effects selection
- **Multi-Part EXR**: On/off preference
- **Snapshot Directory**: RS snapshot source path
- **Standard FPS**: Studio FPS standard for QC check #11 (default 25, key `standard_fps`). A project `sentinel_rules.json` `fps` overrides this and disables the machine control with a "defined by project ruleset" caption (v1.6.0)
- **Texture Repathing presets**: Last 5 Find/Replace pairs (key in `sentinel_settings.json`)
- **Panel Layout**: Window position and docking state (managed by Cinema 4D)

### Per Project (Shared, via `sentinel_rules.json`) — v1.6.0
- **Ruleset**: FPS, start frame, approved presets, default names, safe-area insets, per-check severity (FAIL/WARN), and per-check on/off. Discovered from the scene folder + up to 3 ancestors (nearest wins, no cross-file merge; shadowed files surfaced in the header + QC Report). Precedence: **project rules > machine settings > embedded defaults**. Per-key validation (one bad key is rejected by name, the rest applies). Resolution cached by (path, mtime), invalidated with the QC cache — edits/deletes take effect without restart. Absent file → current defaults. Publishable to a shared folder so a whole team validates against the same rules.

### Per Scene Base (Sidecar JSON next to the `.c4d`, shared across all `_v###` versions)
- **`<base>_history.json`**: Smart Save Version log (comment, QC score, scene stats). As of v1.6.0 each entry is schema v2 `{passed, total, new, accepted}`; older entries render as "X/12 (legacy)"
- **`<base>_notes.json`**: Free-form scene notes + TODO checklist (v1.5.1)
- **`<base>_baseline.json`** (v1.6.0): Accepted QC violations with author + reason, schema v1. Identity = `check_id` + hierarchical path + sibling index + object GUID (parametric checks snapshot the rule value). Merge-on-write (atomic tmp+rename), Synology conflicted-copy detection + merge on load, read-only lockout on an unreadable sidecar. Scene Collector copies + renames it (plus the effective `sentinel_rules.json`) into the delivery
- **Note**: sidecars are copied to the delivery folder by Scene Collector and renamed to the clean delivery base

### Fetched From Scene (Per Document)
- **Shot ID**: Read from Main Take name, synchronized with scene
- **Render Preset**: Read from active render data, matches scene settings

### Runtime Only (Per Session)
- **QC check results**: Cached with 0.5s cooldown, dirty-flag invalidation via CoreMessage
- **Scene stats**: Object count, polygon count, materials, lights

## Current Status (v1.22.0)

### What Works ✅
- **Panel SPA — sección Deliver** (v1.22.0): cuarta sección de contenido del panel dockeable `SentinelPanelSPA` (tras Overview 6.0, QC 6.1, Render 6.2) — tres bloques apilados con status en cabecera. **Version**: `v007 TR · hace 2h · QC 9/12` (última versión) + botón **Save Version** (primary/acento — acción primaria de la sección) + **Recent Versions** (lista de hasta 15 con badge de status color-coded, filtro cliente All/WIP/TR/CR/FINAL, **click en fila → abre/cambia directo, sin confirm**). **Notes**: `⚠ Notes: texto + N TODOs (M pendientes)` + botón **Edit Notes**. **Deliver-access**: Collect Scene (→ abre el Hub en foco `deliver`), Supervisor (→ ventana Reports), Delivery Summary (condicional, solo con manifest colectado junto a la escena → Reports). **Formularios absorbidos como sub-vista** (no ventana): Save Version y Edit Notes cambian el contenido del panel a los componentes `SaveVersionPage`/`NotesPage` existentes (props opcionales `onBack`/`onDone`; `FormPageShell` gana un modo `embedded` — sin `h-screen` ni footer pineado, el submit fluye tras los campos en vez de irse al fondo del panel dockeado alto; host `FormDialog` intacto como fallback) con botón `← Deliver`; submit reutiliza las ops `form/save_version/*` y `form/notes/*` de Fase 4 — cero lógica de submit duplicada. **Abrir una versión es NO DESTRUCTIVO**: `flows.open_version_core` **re-activa la pestaña** si esa versión ya está abierta (`GetFirstDocument` walk + `SetActiveDocument` → `switched`), y si no la abre de disco con `LoadFile` (= File>Open, documento nuevo, el activo se conserva → `opened`); NO hay paso de descarte de cambios (el guard `unsaved_changes`/`force` del review inicial describía un riesgo inexistente y se retiró tras la verificación live — arreglaba el bug real "no puedo cambiar entre escenas legacy abiertas"). Ops `panel_deliver_ops.py`: `panel/deliver` (read-only, bloques AISLADOS igual que overview/render — un fallo no blanquea el resto; fuentes reales de `versioning.py`/`notes.py`/`manifest.py`), `panel/deliver/open_version` (núcleo sin-diálogo, dict de estado en vez de `MessageDialog`, patrón anti-freeze; re-stampea contra el doc ahora-activo), `panel/deliver/open_collect` (Hub foco deliver). SPA: `DeliverSection` presentacional con sub-router local (main/save_version/notes); `PanelPage` posee `deliverState`/`loadDeliver`/poll/stamp igual que render/qc; lógica pura en `panelDeliver.ts` (status lines, filtro recent, badge tone). **Badges de versión con paleta del panel nativo**: WIP=gris · TR=ámbar · CR=azul (token nuevo `--color-status-info`) · FINAL=verde. Pestaña Deliver nativa intacta (retiro es 6.4). pytest 808, vitest 106. **Verificación live VERIFICADA** (C4D 2026.303, usuario, escena real): sección renderiza; abrir/cambiar entre versiones legacy abiertas re-activa la pestaña al instante (sin recarga ni confirm de descarte); Save Version resalta y su submit queda tras los campos; Edit Notes edita; Collect/Supervisor abren sus páginas; badges diferenciados por color. Fixes cazados en vivo y arreglados en la raíz: (1) el plugin instalado seguía pre-6.3 → `sync.sh` a la carpeta de prefs activa `9D810372` (C4D crea carpetas `_p`/`_x` al actualizar, pero la activa era la base); (2) open_version recargaba de disco en vez de activar la pestaña abierta → switch-to-open; (3) confirm de descarte engañoso → retirado (LoadFile no descarta nada); (4) Save Version sin peso visual → primary; (5) submit del form absorbido al fondo del panel → modo embedded; (6) TR/CR mismo tono → CR azul propio.
- **Panel SPA — sección Render** (v1.21.0): dentro del panel dockeable `SentinelPanelSPA`, tercera sección de contenido (tras Overview 6.0 y QC 6.1) — bloques apilados con status en cabecera: Preset (`resolución · fps` + selector + Reset All/Force 9:16 ⚠), Sentinel Frame (`sin tag` / `en <cámara> · N formatos` + Add/Select), AOVs (status `N AOVs` — el modo vive en el switch — con Show AOVs movido a la cabecera del bloque; **Coverage** = botones Essentials/Production que APLICAN de forma aditiva, sin confirm, undoable; **Light Groups** = toggle explícito on/off vía `panel/render/set_light_groups {enabled}`, idempotente set-al-valor-deseado (`no_groups_assigned` → toast, no un falso éxito) — ya NO es un valor de tier; **Output** = switch de dos opciones `Multi-Part EXR / Direct output` vía `panel/render/set_multipart {enabled}` (sustituye al antiguo `toggle_multipart`)), Snapshots (dir efectivo + chip origen auto/manual + Save Still + Open Folder + toggle Watch), Post-Render (último informe / sin informe + Validate → deep-link a Reports). Show AOVs resuelve nombres amigables (Beauty, GI, Diffuse Lighting…) vía `aovs.aov_type_name` (mapa inverso de `_AOV_DEFS`, con fallback `AOV #N` para tipos no mapeados) — antes mostraba el número de tipo RS en crudo. Ops `panel_render_ops.py`: `panel/render` read-only con bloques AISLADOS (un fallo no blanquea el resto, mismo patrón que `panel/overview`); mutaciones reutilizan los motores nativos vía núcleos sin-diálogo extraídos (`_apply_preset_core`, `_force_render_settings_core`, `_toggle_aspect_core`, `_add_sentinel_frame_tag_core`, `_toggle_light_groups_core`, `snapshot_save_still_core`, `snapshot_open_folder_core`) — un `MessageDialog` dentro del drain de la cola congelaría todo C4D, así que cada wrapper nativo con diálogo queda intacto y el op llama solo al núcleo (dict de estado). El confirm gate es **solo** para `reset_all` + `force_vertical` (genuinamente destructivas); `aov_tier` (essentials/production) y los toggles de light_groups/multipart NO piden confirm (aditivos/idempotentes, revertibles con Cmd+Z) — `isDestructiveRenderOp` refleja esto. Pestaña Render nativa intacta (retiro es 6.4). pytest 794, vitest 93. **Verificación live VERIFICADA** (C4D 2026.302, MCP + usuario, SHOT_18): 5 bloques con status correcto; add_frame_tag en cámara real (format_count poblado) sin freeze; reset_all/aov_tier/save_still verificados SIN modal/Picture-Viewer desde la ruta del op (el riesgo de congelar C4D queda cerrado); confirm_required solo en reset_all/force_vertical; set_light_groups idempotente + flip real en ambas direcciones; Show AOVs con nombres; switch Multi-Part funcional; Cmd+Z revierte una mutación de frame-tag. Fixes cazados en vivo: números de tipo AOV→nombres, checkbox Multi-Part→switch explícito, reorg del bloque AOVs (botones Coverage / toggle Light Groups / switch Output), confirm retirado de los tiers aditivos.
- **Panel SPA — sección QC** (v1.20.0): dentro del panel dockeable `SentinelPanelSPA` (v1.19.0), la sección QC reemplaza el placeholder — tarjetas FAIL (rojo) → WARN (ámbar, más ligeras) → línea plegada `▸ N OK · M disabled`, acción directa por tarjeta (Select/Fix/Info/Accept inline, sin popups) y Fix-all. Reutiliza el mismo scoring compartido que Overview/Reports en una sola pasada (`panel/qc`); las mutaciones (`panel/qc/select`, `panel/qc/accept`, `panel/qc/fix_all`) reusan exactamente las ramas de `_op_form_gate_submit` (baseline accept, `apply_fixes` en un undo) e invalidan la caché. Select cicla uno-por-click (cursor por-check), como el nativo. Fix respeta el contrato de confirmación del palette para acciones destructivas (materiales/FPS). La pestaña QC nativa queda intacta (estrategia paralela, retiro previsto en fase 6.4). **Verificación live VERIFICADA** (C4D 2026.302, MCP + usuario): sección QC renderiza, accept sella baseline y sube score, select cicla, fix refresca, sin popups; más los fixes cazados en vivo (crash React #31 detail-lista, accept-sin-guardar, denominador=total, stills por token — RS-LookDev score 6/12→11/12). pytest 730, vitest 72.
- **All 12 Quality Checks**: Lights, visibility, keyframes, camera shift, presets, assets/textures, unused materials, default names, output paths, take validation, FPS/frame range, cross-aspect safe area
- **Auto-fix**: Lights→group, camera shift→reset, unused mats→delete, FPS/range→studio standard (all presets at once with confirmation)
- **Modular `sentinel/` package** (v1.6.0): `.pyp` is a 115-line bootstrap; engine (`checks/`, `qc/`, `rules.py`, `baseline.py`, workflow engines) and UI (`ui/`) are importable modules. Reload policy = restart C4D (no `sys.modules` purge — see Development Flow). Verification ladder: pytest 70/70 + frozen fixture oracle in C4D 2026.3
- **QC Check Registry** (v1.6.0): the 12 checks are declarative entries in `qc/registry.py` (id, label, severity FAIL/WARN, fix capability, params); panel, QC Report, Save Version summary and Collector preflight all iterate the registry — a 13th check costs one entry, not N edits. Severity is display-only in v1 (equal score weight)
- **Per-Project Rules** (v1.6.0): `sentinel_rules.json` sets FPS, start frame, approved presets, default names, safe-area insets, per-check severity and on/off. Discovered from the scene folder + up to 3 ancestors (nearest wins; shadowed files surfaced), precedence project > machine > defaults, per-key validation, live re-resolution on edit/delete. Header shows the active ruleset; a disabled check dims and drops out of the score denominator (`X/11 · 1 disabled`)
- **Baseline / Accepted Violations** (v1.6.0): accept known violations from the panel with mandatory author + reason (`<base>_baseline.json`, schema v1); the score then counts only **new** violations, a mixed row reads `N new (M accepted)`. Identity = path + sibling index + object GUID (delete/rename re-arms, never mis-inherits); merge-on-write with Synology conflicted-copy merge; accept/retire invalidates the QC cache; Collector transports the sidecar + effective ruleset into the delivery
- **Smart Save Version**: Versioned saves (`scene_v###.c4d`) with required comment, QC score, scene stats, sidecar `<scene>_history.json` log
- **Review Status Tags**: WIP / TR (Team Review) / CR (Client Review) / FINAL / Custom → suffix in filename (`scene_v007_TR.c4d`)
- **"Continue from this version"**: After saving a TR/CR/FINAL, offers to auto-create a new WIP version so the review snapshot stays untouched
- **"Last version" pillbox**: Live caption above Save Version showing `v007 TR · 2h ago`
- **Browse Recent Versions**: Inline list of last 5 versions in the panel with status badges (color-coded), filter dropdown (All/WIP/TR/CR/FINAL), click row to open
- **Scene Notes & TODOs**: Per-scene sidecar JSON (`<base>_notes.json`) with free-form notes + checklist of TODOs. Modal editor with checkbox toggle + delete. Live caption in panel ("⚠ Notes: text + 3 TODOs (2 pending)"). Notes shared across all versions of the same scene base. Included in QC report export and Scene Collector manifest. Sidecar copied to delivery folder.
- **Scene Collector — clean delivery naming**: Renames the collected `.c4d` to the original scene base (stripping `_v###[_status]`) so deliveries have clean identity (e.g., `robot_010_v022_FINAL.c4d` → `robot_010.c4d`). Manifest preserves traceability via `original_filename`, `original_version`, `original_status`
- **Tabbed UI** (v1.5.2): Scene Header (filename + Shot/Artist + QC bar) always visible above 4 tabs (QC / Render / Versions / Tools); footer (GitHub / Report Bug) always visible below. Tab content is dynamically rebuilt via `LayoutFlushGroup` — only the active tab lives in the layout (HideElement does not collapse layout space in C4D 2026)
- **QC Report Export**: JSON with score, scene stats, all check details
- **RS AOV Management**: Essentials (11) / Production (17+) tiers, per-compositor config (Nuke vs AE)
- **Light Groups AOV**: Diagnose + toggle on Beauty AOV
- **Scene Collector**: Pre-flight QC + SaveProject() + manifest JSON
- **Take Validation**: Camera per take, $take token in output paths
- **Render Presets**: Dropdown with resolution display, Reset All from template, Force 9:16 toggle
- **Multi-Format Render Setup** (v1.5.4, refactored v1.5.5): One-click child-Take generator for the 5 standard delivery aspects (16:9, 9:16, 1:1, 4:5, 21:9). Each Take gets a cloned Render Data with format-specific resolution + output path. **Composition Mode** dropdown chooses between (a) "None" — camera unchanged, only resolution/output overrides (default, matches GSG Social Frame plugin); or (b) "Resize Canvas" — sensor-size override per format using AR_ResizeCanvas math (`new_aperture = src × target_w / src_w`), safer than focal-length override (doesn't break zoom animations or DOF). Idempotent; explicit `take.SetCamera` + `SetParameter` after `FindOrAddOverrideParam` (defends against find-OR-add silent skip); cleans up stale FOV/focal/aperture overrides on re-run. Full undo wrapping; summary dialog with composition mode
- **Cross-Aspect Safe Area** (v1.5.5): QC #12 verifies opt-in marked subjects stay inside per-format safe-area regions across all active Multi-Format delivery Takes. **Crop interpretation** model: bbox projects once into the master Take's NDC, then each format's safe area is computed as a centered crop region with per-side insets (e.g. 9:16 inset 8/15/5/10 for IG Reels caption + icon stack). Auto-refresh uses current frame (cheap); Info button runs full keyframe sweep (PSR keyframes + midpoints, original timeline position restored via try/finally). Tools tab → "Mark / Unmark Safe Area Subject" smart-toggle button drives the UserData marker. Sample frame violations reported per (object × format × frames + edges)
- **Safe-Area Viewport Overlay** (v1.5.6): Live colored rectangles rendered in the active camera viewport showing each multi-format Take's crop region (16x9 white, 9x16 orange, 1x1 cyan, 4x5 magenta, 21x9 yellow). Toggle in Render tab auto-creates a managed `SafeAreaOverlayObject` (ObjectData) marker at scene root; persists with `.c4d` save. Same crop-interpretation math as QC #12. Implementation pivoted from `SceneHookData` (removed in C4D 2026) → `TagData.Draw` (registers but never invoked by viewport pipeline) → `ObjectData.Draw` in `DRAWPASS_OBJECT` (confirmed working, fires regardless of selection)
- **Texture Repathing** (v1.5.7): Multi-renderer bulk find/replace + smart-fix tool for texture paths. Tools tab → "Asset Management" → "Texture Repathing..." (also reachable from QC #6 Assets Info). Async dialog (keeps C4D interactive so Cmd+Z works). Comprehensive `scan_all_texture_paths` covers RS/Arnold maxon node graphs, classic Xbitmap shader chains, Octane legacy image shaders, material/object BaseContainer params, RS Dome Light HDR (compound DescID), Alembic caches, and tag shader chains (Octane Environment Tag). Per-path status: OK / absolute / missing / asset_uri / empty (`relative://` resolved by searching common subdirs like Redshift does). Scrollable `TextureListArea` UserArea (ScrollGroup-backed) with status filter. Bulk Find/Replace (case-insensitive default + Match case toggle), last-5 presets persisted in `sentinel_settings.json`. Smart Actions: Auto-Find Missing, Make All Relative, Clear pending. Per-row `[...]` file picker. Apply All wraps the batch in one undo step (`StartUndo`/`EndUndo` + node-graph `doc.AddUndo(UNDOTYPE_CHANGE, mat)` anchor so the maxon `UndoMode.ADD` transaction joins the document undo). Auto-refreshes on scene change via `CoreMessage`/`Timer`. **Superseded in v1.11.0 by the Asset Hub** (dialog class kept in code, retired from the UI)
- **Asset Hub** (v1.11.0): Unified dialog (`AssetHubDialog`) replacing the Texture Repathing and Collect Scene entry points — full asset inventory merging the structured texture scan with `GetAllAssetsNew` (the document's own file filtered out of the results), status `ok`/`missing`/`absolute`/`asset_uri`/`empty`, per-asset size + running totals, clickable provenance (used-by → selects the owning material/object in the scene), lazy thumbnails (batched in a `Timer`, capped cache, no-retry placeholders). Bulk Find/Replace plus Search Folder for Missing (ambiguity-safe basename matching, never auto-picks) repath every shader sharing a path in **one** undo step. Embedded pre-flight QC strip (Fix/Accept/Details reusing `apply_fixes` + `BaselineActionDialog`, no chained MessageDialogs). Delivery bar with a missing-asset gate (warns, lets the artist continue — the manifest still seals them as missing) and Folder/Zip output (stdlib `zipfile`, source folder always kept), plus an in-Hub delivery summary. Engine lives in pure-stdlib `assets.py` (pytest-covered: merge/dedupe, status/type classification, totals, missing-match); `collect_scene` refactored into `run_collect_pipeline` — byte-equivalent to the legacy path, the v1.10 sealed manifest untouched
- **Sentinel Reports + web UI foundation** (v1.13.0): Embedded HTML surface inside a dockable GeDialog (`CUSTOMGUI_HTMLVIEWER`, verified modern WebKit in C4D 2026.302 — full CSS/JS, and Cmd+Z passes through to C4D with the webview focused). Architecture (Overseer-pattern, own implementation): stdlib `ThreadingHTTPServer` on 127.0.0.1 (ports 8347-8356) serving the built SPA from `plugin/web/` + `/api/<op>`; `MainThreadQueue` drained by the dialog `Timer(25ms)` so every op runs on C4D's main thread (dispatch must stay read-only/idempotent). Pure bridge in `webbridge.py` (no c4d import, symlink-hardened static serving, pytest-covered incl. `delivery_report_payload` manifest→SPA mapper). SPA: Vite+React+TS+Tailwind v4 in `web/` (source; node = dev-only), build committed to `plugin/web/`; design system from `docs/design/DESIGN.md` (Linear-adapted tokens: canvas #101113, accent #5e6ad2, status colors exclusive to state; Inter packaged locally, OFL). First page: **Delivery Summary** (KPI cards + asset table with badges) reading the real `sentinel_manifest.json` of the active collected package — the Versions-tab "Delivery Summary..." button opens it (legacy dialog kept as fallback). Redesign spec + phases 2-5 in `docs/superpowers/specs/2026-07-18-ui-redesign-design.md`
- **Snapshot System**: Cross-platform EXR→PNG with full ACES pipeline (ACEScg→sRGB)
- **Snapshot Watchfolder** (v1.12.0): Opt-in auto-convert — panel toggle (default OFF, persisted) watches the RS snapshot dir from the panel `Timer` (2.5s throttle). Settle rule = two consecutive scans with identical (mtime, size) — never converts half-written files; pre-existing backlog is primed as processed on enable (only NEW snapshots convert). Silent pipeline (`snapshot_auto_convert`: no dialogs, no Picture Viewer — nothing blocks the Timer) reusing the ACES EXR→PNG converter into the artist stills dir; caption reports each conversion. Non-EXR alert: if the newest file in the watch dir is not an `.exr`, the caption warns that RenderView lost the "Save snapshots as EXR" setting (session-only Qt state, documented limitation). Pure engine `scan_snapshot_candidates` in `snapshots.py` (registry-based, importable without c4d, 12 tests)
- **Scene Tools**: Hierarchy, H→Layers, Solo Layers, Drop to Floor, Vibrate Null, ABC Retime, Camera Rigs
- **CoreMessage dirty-flag**: Instant scene change detection, no polling waste
- **Cross-platform**: macOS + Windows (platform-aware file opener, Python discovery)

### Known Limitations ❌
- **Forcing Redshift Snapshot Directory**: Can't override Redshift's save location at runtime
- **Programmatic Snapshot Triggering**: No API access to trigger snapshots from code
- **Redshift must be configured manually**: RenderView → Preferences → Snapshots → EXR format. **Verified unfixable (2026-07-10)**: the "Save snapshots as EXR" checkbox is session-only Qt state (widget `snapshotsAsEXRCBox`). RenderView persists state to `prefs/redshift_rv.cfg`, but that file has NO key for the EXR toggle, and injecting one (`saveSnapshotsAsEXR`, a string present in the RS binary) gets STRIPPED at startup — the loader round-trips only its known key set. No plist/QSettings store either. Must be re-ticked per RenderView session; no automation lever exists.
- **C4D docked panel does not auto-shrink**: When tab content gets smaller (e.g., switching from QC → Versions), the panel window stays at its taller size. This is a confirmed C4D 2026 framework limitation (no `SetSize`/`ResizeWindow`/`FitToContent` API for docked panels — Maxon staff confirmed). Even Maxon's own panels (Take Manager, AOV Manager) have this behavior. The user must drag-resize manually if they want compact mode. Our `BFV_SCALEFIT` spacers absorb the gap correctly within the layout, but the window frame itself stays put.
- **No native tooltips on widgets**: `BFM_GETCURSORINFO` is not routed to embedded `GeUserArea` in C4D 2026 Python. Section titles include hints (e.g., "click any row...") instead of hover tooltips.
- **C4D 2026 deprecated plugin types**: `c4d.plugins.SceneHookData` and `RegisterSceneHookPlugin` were removed/migrated in C4D 2026 (verified empirically — local 2026 SDK clone has zero references). Sentinel uses `ObjectData.Draw` in `DRAWPASS_OBJECT` for the v1.5.6 Safe-Area Overlay (a scene-root marker, no camera needed) and `TagData.Draw` for the v1.8.0 Sentinel Frame per-camera tag — both confirmed working.
- **CORRECTION (v1.8.0): `TagData.Draw` DOES fire in C4D 2026.** The v1.5.6 note that "`TagData.Draw` is never invoked" was WRONG — the root cause was a missing registration flag, not a removed API. `TagData.Draw` fires reliably when the tag is registered with `info = c4d.TAG_VISIBLE | c4d.TAG_EXPRESSION | c4d.TAG_IMPLEMENTS_DRAW_FUNCTION` (the flag `TAG_IMPLEMENTS_DRAW_FUNCTION` = 256 is REQUIRED; verified live in C4D 2026.301, and confirmed against the working C4DMultiFrame prototype + Maxon staff on developers.maxon.net/forum/topic/12708). Draw signature `Draw(self, tag, op, bd, bh) -> bool`; gate with `if bd.GetDrawPass() != c4d.DRAWPASS_OBJECT: return True` (else it draws 3–4× per frame), filter viewport with `bd.GetSceneCamera(doc) == op`, and treat the draw thread as READ-ONLY (the tag/node come from a CLONED document — Python attributes set via `setattr` do NOT survive the clone; per-node state Draw reads must live in the BaseContainer/params). So an always-on drawer on a camera can be a plain TagData; the ObjectData-marker workaround is only needed for scene-root drawing with no host object.

## Active Tasks
See `ROADMAP.md` for the full feature roadmap and pending phases (v1.5.0, v1.6.0, backlog).

`docs/solutions/` — documented solutions to past problems (bugs, workflow patterns), organized by category with YAML frontmatter (`module`, `tags`, `problem_type`). Relevant when implementing or debugging in documented areas; hard-won C4D API lessons also live in `docs/audit/` and the KTD sections of `docs/plans/`.

## Do NOT:
- Create multiple versions of the same file
- Add complex dependency management
- Create installation/setup scripts (unless updating the existing one)
- Promise automatic features that require Redshift API access we don't have
- Over-complicate the solution

## Keep It Simple
The plugin should do what it can do well, and clearly communicate its limitations.

## External References (C4D / Redshift SDK)

When writing new C4D or Redshift code, consult these references **before inventing patterns from scratch**. All are located outside the plugin repo, in the sibling folder `../11 C4D DEV/`.

### 1. Maxon Official Python API Examples (local clone)
- **Path**: `../11 C4D DEV/Cinema-4D-Python-API-Examples/`
- **Upstream**: https://github.com/Maxon-Computer/Cinema-4D-Python-API-Examples (Apache-2.0, maintained by Ferdinand Hoppe @ Maxon)
- **Covers**: Official plugin hooks, GUI patterns, token system, mograph, volumes, node graphs, 2024/2026 examples (persistent dialogs, bidirectional scene updates, OCIO, licensing, render tokens)
- **Use for**: Any generic C4D Python API pattern — GUI (GeDialog), tokens, CoreMessage, threading, node graph (maxon.GraphNode), file I/O

### 2. renderEngine — Community Render Engine Wrapper (local clone)
- **Path**: `../11 C4D DEV/renderEngine/`
- **Upstream**: https://github.com/DunHouGo/renderEngine
- **⚠️ No license** — read and learn from it, but do NOT copy code verbatim into Sentinel without asking permission
- **Covers**: Redshift (AOVs, materials, scene lights), plus Arnold/Octane/Vray/Corona/CentiLeo
- **Use for**: Redshift API patterns (the official Redshift Python API has no public docs). Key files:
  - `Redshift/aov.py` — AOV helper (cross-check our hard-coded IDs)
  - `Redshift/material.py` — RS material node graph wrapper
  - `Redshift/scene.py` — RS lights, tags, proxies, HDR dome
  - `constants/` — REDSHIFT_AOVS and description IDs
  - `utils/node_helper.py` — `NodeGraghHelper` for maxon.GraphNode

### 3. Official Maxon Python Docs (online)
- **URL**: https://developers.maxon.net/docs/py
- **Use for**: Written manuals and full API index. Pair with the local examples clone for complete coverage.

### 4. C4D Window / Manager IDs (undocumented)
Not in `symbol.h`. Obtained empirically via `FindShortcutAssign`. Verify in live C4D before relying on them.
```python
MATERIAL_MANAGER        = 150041
OBJECT_MANAGER          = 100004709
LAYER_MANAGER           = 100004704
PICTURE_VIEWER          = 430000700
ATTRIBUTE_MANAGER       = 1000468
NODEEDITOR_MANAGER      = 465002211
TAKE_MANAGER            = 431000053
TIMELINE_MANAGER        = 465001516
XPRESSO_MANAGER         = 1001148
RENDER_QUEUE            = 465003500
RENDER_SETTING          = 12161
ASSET_BROWSER           = 1054225
PROJECT_ASSET_INSPECTOR = 1029486
CONSOLE                 = 10214
VIEWPORT                = 59000
ARNOLD_IPR              = 1032195
ARNOLD_SHADER_NETWORK   = 1033989
CORONA_NODE_MANAGER     = 1040908
```
Source: https://github.com/DunHouGo/cinema4d_Shortcut (not cloned, too small).

### Updating the local clones
Manual pull when needed (Maxon updates ~monthly, renderEngine sporadically):
```bash
cd "../11 C4D DEV/Cinema-4D-Python-API-Examples" && git pull
cd "../11 C4D DEV/renderEngine" && git pull
```

## Version History Summary
- **v1.22.0** (Jul 2026): **UI Fase 6.3 — panel SPA sección Deliver**. Cuarta sección de contenido del panel dockeable (tras Overview 6.0, QC 6.1, Render 6.2): tres bloques apilados con status en cabecera — **Version** (última versión + Save Version + Recent Versions con badges color-coded, filtro cliente y click-para-abrir), **Notes** (summary + Edit Notes) y **Deliver-access** (Collect→Hub foco deliver, Supervisor→Reports, Delivery Summary condicional→Reports). **Decisión de diseño clave**: los formularios Save Version y Notes se **absorben como sub-vista dentro del panel** (no ventana) — al pulsar el botón, el contenido del panel cambia al componente `SaveVersionPage`/`NotesPage` COMPLETO (que ya existía de Fase 4) montado inline con props opcionales `onBack`/`onDone` (default no-op → el host `FormDialog` nativo sigue funcionando como fallback), con botón `← Deliver`; al guardar/cancelar vuelve a main y re-fetchea. Submit reutiliza las ops `form/save_version/*` y `form/notes/*` — cero lógica de submit duplicada. Ops nuevas en `panel_deliver_ops.py` (adaptadores finos): `panel/deliver` (read-only, bloques AISLADOS igual que `panel/overview`/`panel/render` — un bloque que revienta no blanquea los otros; fuentes reales de `versioning.py`/`notes.py`/`manifest.py`, nada inventado; `unsaved` cuando el doc no tiene path; `recent` capado a 15 con `path` absoluto por fila), `panel/deliver/open_version {path, force?}` (abre un `.c4d` vía **núcleo sin-diálogo** `flows.open_version_core` — mismo patrón anti-freeze de v1.21.0: un `MessageDialog` dentro del drain de la cola congela TODO C4D, así que el núcleo devuelve un dict de estado con las guardas espejo del `_on_history_row_click` nativo — `bad_path`/`file_not_found`/`already_active`/`unsaved_changes`→force/`load_failed` — y el wrapper nativo conserva sus diálogos; test `_forbid_dialog` prohíbe MessageDialog Y QuestionDialog en la ruta del op; el op cablea `force` para el re-confirm de cambios sin guardar), `panel/deliver/open_collect` (Hub foco deliver). SPA: `DeliverSection` presentacional con sub-router local (main/save_version/notes); `PanelPage` posee `deliverState`/`loadDeliver`/polling/stamp exactamente como render/qc (fetch al entrar en la sección + refetch por cambio de stamp, `stampRef` re-anclado); lógica pura en `panelDeliver.ts` (status lines por bloque, `filterRecent` con `""`=WIP como en `versioning.py`, `statusBadgeTone` → tokens wip/tr/cr/final, nunca el acento) + vitest. `fetchPanelDeliver` degrada a `PanelDeliverState` all-null en fallo (el op nunca emite envelope de error) → los bloques null renderizan "unavailable", sin `.catch`. Mocks con la FORMA anidada real (lección React #31). Pestaña Deliver nativa intacta (retiro es 6.4). Verificación: pytest 808 + vitest 106 + reviews adversariales por tarea + review final opus (Important cazado: `fetchPanelDeliver` sin guard de envelope `{error}` → crash panel en blanco; arreglado). **Pasada de diseño de producto tras verificación live** (C4D 2026.303, usuario, escena real): (1) `open_version` **re-activa la pestaña ya abierta** (`SetActiveDocument`) en vez de recargar de disco — arregla el bug "no puedo cambiar entre escenas legacy abiertas"; el guard `unsaved_changes`/`force` se retiró (LoadFile abre un doc nuevo, no descarta nada) y el confirm de open-version desapareció (click = abrir/cambiar directo + toast `switched`/`opened`); (2) Save Version → primary; (3) `FormPageShell` modo `embedded` para que el submit del form absorbido no se pinee al fondo del panel alto; (4) badges con paleta nativa WIP=gris/TR=ámbar/CR=azul(`--color-status-info`)/FINAL=verde. Descubierto en vivo: el plugin instalado seguía pre-6.3 (sync a la carpeta de prefs activa `9D810372`).
- **v1.21.0** (Jul 2026): **UI Fase 6.2 — panel SPA sección Render**. Tercera sección de contenido del panel dockeable (tras Overview 6.0 y QC 6.1): bloques apilados con status en cabecera y acción directa por bloque, sin popups — Preset → Sentinel Frame → AOVs → Snapshots → Post-Render (orden fijo, aprobado en brainstorm). Ops nuevas en `panel_render_ops.py`: `panel/render` (read-only, bloques AISLADOS igual que `panel/overview` — un bloque que revienta no blanquea los otros cuatro); mutaciones: `set_preset`/`reset_all`⚠/`force_vertical`⚠ (presets), `add_frame_tag`/`select_frame_tag` (Sentinel Frame), `toggle_watchfolder`/`save_still`/`open_folder` (snapshots). **Bloque AOVs reorganizado por estructura real de acción** (fix post-build tras live): la línea de status es `N AOVs` (solo el conteo — el modo vive en el switch); **Coverage** = botones Essentials/Production que APLICAN (aditivo, sin confirm, revertible); **Light Groups** = toggle explícito on/off vía op nueva `panel/render/set_light_groups {enabled}` (set-al-valor-deseado idempotente; `no_groups_assigned` → toast, no falso éxito) — YA NO es un valor de `aov_tier`; **Output** = switch de dos opciones `Multi-Part EXR / Direct output` vía `panel/render/set_multipart {enabled}` (sustituye al `toggle_multipart` original, retirado). Show AOVs se movió a la cabecera del bloque y ahora resuelve **nombres** (Beauty, GI, Diffuse Lighting…) vía `aovs.aov_type_name` (mapa inverso de `_AOV_DEFS`, fallback `AOV #N`) en vez de mostrar el número de tipo RS en crudo. **Confirm gate ahora SOLO en `reset_all` + `force_vertical`** (genuinamente destructivas); `aov_tier` (essentials/production) y los toggles light_groups/multipart NO piden confirm (aditivos/idempotentes, Cmd+Z-ables) — `isDestructiveRenderOp` refleja esto. **Patrón clave** (se mantiene): se extrajeron núcleos SIN DIÁLOGO de los motores nativos (`_apply_preset_core`, `_force_render_settings_core`, `_toggle_aspect_core`, `_add_sentinel_frame_tag_core`, `_toggle_light_groups_core`, `snapshot_save_still_core`, `snapshot_open_folder_core`) — más extracciones de las que anticipaba la spec (solo mencionaba preset/reset-all como candidatos acoplados a `QuestionDialog`), mismo motivo de seguridad: un `MessageDialog` disparado dentro del drain de la cola del panel congelaría TODO C4D, así que el wrapper nativo con diálogo queda intacto y el op llama solo al núcleo (devuelve un dict de estado); tests `_forbid_dialog` verifican cero modales en la ruta del op. Validate = deep-link a Reports → Render Validation (visible y habilitado incluso cuando el bloque Post-Render está `unavailable` — sin informe previo o doc sin guardar — porque el deep-link no depende del contenido del bloque). SPA: `RenderSection` con `RenderBlock` genérico compartido entre los 5 bloques, status por bloque (`panelRender.ts`, pura), confirm inline para las 2 destructivas (paridad QC/Overview), `load(true)` tras cada mutación para re-anclar el stamp. Frame block expone `format_count` (formatos activos del Sentinel Frame). Pestaña Render nativa intacta (retiro es 6.4). Verificación: pytest 794 + vitest 93 + reviews adversariales por tarea. **Verificación live VERIFICADA** (C4D 2026.302, MCP + usuario, SHOT_18): 5 bloques con status correcto; add_frame_tag en cámara real (format_count poblado) sin freeze; reset_all/aov_tier/save_still confirmados SIN modal/Picture-Viewer desde la ruta del op (el riesgo de congelar C4D, el objetivo entero de la extracción, queda cerrado); confirm_required solo en reset_all/force_vertical; set_light_groups idempotente + flip real en ambas direcciones; Show AOVs con nombres; switch Multi-Part funcional; Cmd+Z revierte una mutación de frame-tag. Fixes cazados en vivo: números de tipo AOV→nombres, checkbox Multi-Part→switch explícito, reorg del bloque AOVs (botones Coverage / toggle Light Groups / switch Output), confirm retirado de los tiers aditivos.
- **v1.20.0** (Jul 2026): **UI Fase 6.1 — panel SPA sección QC**. Segunda sección del panel dockeable (tras Overview, v1.19.0): layout "C refinada" — tarjetas FAIL (rojo) → WARN (ámbar, más ligeras) → línea plegada `▸ N OK · M disabled`, acción directa por tarjeta sin popups. Ops nuevas en `panel_ops.py`: `panel/qc` (read-only, reutiliza el scoring compartido `active_rules_for_doc → run_all_checks → compute_score → webbridge.qc_report_payload` en UNA sola pasada, agrupa por severidad vía `webbridge.group_qc_by_severity` — pura, extraída para test directo), `panel/qc/select` (selección en escena — cicla uno-por-click, cursor por-check, igual que el ciclo nativo), `panel/qc/accept` (sella el check en baseline con autor+razón obligatorios, copia exacta de la rama accept de `_op_form_gate_submit`, invalida `check_cache`), `panel/qc/fix_all` (`fixes.apply_fixes` en un solo undo, copia de la rama fix_all vía `_gate_fix_payload`). Cero lógica duplicada — `can_select`/`can_fix`/`fix_action_id`/severidad vienen de `CHECK_REGISTRY`, no inventados. SPA: `QcSection` + `QcCard` (Info expandible, formulario Accept inline con autor pre-rellenado + razón, Fix respeta enabled/reason+confirm del palette igual que Overview, Select), botón Fix-all-fixables; lógica pura en `panelQc.ts`. La pestaña QC nativa queda intacta (estrategia paralela; retiro es fase 6.4). Verificación: pytest 730 + vitest 72 + reviews adversariales por tarea + **verificación live VERIFICADA** (C4D 2026.302, MCP + usuario): sección QC renderiza, accept sella baseline y sube score, select cicla, fix refresca, sin popups; fixes cazados en vivo (crash React #31 detail-lista, accept-sin-guardar, denominador=total, stills por token — RS-LookDev score 6/12→11/12).
- **v1.19.0** (Jul 2026): **UI Fase 6.0 — panel SPA dockeable: host + shell + Overview**. Nuevo panel dockeable **en paralelo** al nativo (que queda intacto y operativo): `SentinelPanelSPA` (comando "Sentinel Panel (SPA)", plugin ID 2099076), un `CUSTOMGUI_HTMLVIEWER` a pantalla completa (`?page=panel`), `Execute`/`RestoreLayout` espejo de `YSPanelCmd` (dockeable + persistencia de layout), `Timer` = drain de la cola + `pump_jobs`. Motor fino `panel_ops.py` sobre los motores existentes — cero lógica duplicada: `panel/state_stamp` (reutiliza `_stamp_for` del Hub), `panel/overview` (dashboard "salud del shot" con bloques scene/qc/assets/render/deliver **aislados**: un fallo de un subsistema no blanquea el resto), `panel/open_form` (abre los formularios nativos). QC = mismo scoring compartido que Reports; assets = pipeline del Hub. SPA: `PanelPage` con **rail adaptativo** (ResizeObserver, <560px rail de iconos con badges de estado / ≥560px sidebar etiquetada), header (escena/versión/barra QC) y **dashboard de 4 tarjetas** (QC con top-fails + quick-fixes que respetan enabled/reason del palette · Assets missing/tamaño/VRAM · Render · Versión/TODOs) con deep-links a Hub/Reports y polling de stamp cada 2s. Spike PostWebMessage resuelto en el spec: push C4D→JS descartado (no-op verificado), refresco por polling (patrón Hub). IA aprobada en brainstorm con companion visual (spec madre `2026-07-21-panel-spa-design.md`). **Spike de dock VERDE (verificado en vivo, usuario)**: dockea como panel nativo, el breakpoint del rail (560px) responde al resize, Cmd+Z atraviesa al documento con el panel dockeado con foco, el polling refresca el dashboard — probado con escena real de producción (SHOT_18): overview correcto con QC 6/12 y 39 assets, coincidiendo con Reports/Hub. Honesto: las secciones QC/Render/Deliver/Tools del dashboard siguen siendo placeholders — su contenido real es trabajo de 6.1-6.4, no de este release. Verificación: pytest 690 + vitest 60 + build committeado.
- **v1.18.0** (Jul 2026): **UI Fase 5.2 — Asset Hub: Shrink + Copy into project; Fase 5.3 — conmutador de variantes de resolución**. Cierra las dos acciones de Overseer con valor de artista que quedaron en backlog tras la Fase 5.1. **Selección múltiple** en el Hub: click único, cmd/ctrl+click toggle, shift+click rango sobre la lista visible (ordenada+filtrada+facetada), `Escape` limpia, contador "N selected" en la toolbar; lógica pura `applySelection` en `hubTable.ts` (vitest). Relink Selected se gatea a exactamente 1 seleccionada. **Shrink por objetivo en K** (4K/2K/1K, lado mayor, no porcentajes): planners puros `shrink_plan`/`shrink_target_name` en `assets.py` (razones de skip `not_ok`/`not_image`/`no_meta`/`already_small`, VRAM antes/después vía `imagemeta.vram_bytes`), diálogo inline con preview espejo en TS (`shrinkPreview`, vitest verificado contra los mismos números que `tests/test_assets.py`). Corre como **job con progreso** reutilizando el slot único de `JobRegistry`/`hub/job_status` compartido con collect (`job_running` si se solapan): por archivo, copia hermana `<stem>_<K>.<ext>` con el saver correspondiente a la extensión original (allowlist png/jpg/tif/bmp/tga — EXR/HDR excluidos deliberadamente, nunca degradación silenciosa) dejando el original intacto, luego relink de todos los shaders que comparten la ruta. Todo el relink del lote va en **un solo** `StartUndo`/`EndUndo` (un Cmd+Z restaura los originales); fallos de escritura por archivo se acumulan como error de fila y nunca abortan el lote (`_settle_relink_results`, patrón `apply_repath`). **Copy into project**: `copy_plan` puro (basename cross-platform, detección fuera-de-proyecto por prefijo de ruta normalizada), op síncrona `hub/copy_into_project` que copia a `<docdir>/tex/` con colisión segura (mismo tamaño en bytes → reusa, distinto → error de fila, nunca sobrescribe) y relink en un único undo. Ambos flujos: toast de resumen, re-fetch de inventario + meta, stamp re-anclado. Motor puro cubierto en pytest, ops con contrato via harness fake-c4d. **Desviaciones de implementación** (ver spec): el gate de habilitación de los botones Shrink/Copy en la toolbar es deliberadamente **grosero** (solo status/asset_type de la selección) — la elegibilidad exacta contra el `target_px` elegido vive en `shrinkPreview`, calculada dentro del diálogo; y el preview en TS usa `Math.round` mientras el servidor usa `round()` de Python (round-half-to-even) — mismo resultado en la inmensa mayoría de los tamaños reales, diverge solo en el caso exacto de `.5` en el escalado, sin impacto práctico documentado. Verificación (5.2): pytest 630 + vitest 43 + reviews adversariales por tarea.

**Fase 5.3 — conmutador de variantes de resolución**: nace del uso real — packs comerciales con varias resoluciones en disco (`plaster_4k_1.jpg`/`plaster_8k_1.jpg`) y los proxies `_2K` que genera el propio Shrink de 5.2 deben poder conmutarse alta ↔ proxy sin repathing manual. Motor puro en `assets.py`: `split_res_token(basename)` reconoce el token de resolución (mapa `1k→1024 … 16k→16384`), delimitado por inicio/fin de nombre, `_`, `-` o `.` (nunca dentro de palabra), tomando el ÚLTIMO token si hay varios (`4k_scan_2k.png` conmuta el `2k`) — cubre tanto el sufijo propio del Shrink como tokens genéricos de packs comerciales; `find_res_variants(records, list_dir)` agrupa por directorio (listado cacheado por llamada, `list_dir` inyectable) hermanos con mismo `(prefix, suffix)` case-insensitive y separadores normalizados cross-platform, solo devuelve grupos con ≥2 variantes, orden por px desc. Ops: `hub/variants` (read-only, mismo cap de lote 64 que `hub/meta`) resuelve key→variantes vía `_THUMB_PATHS`; `hub/switch_res {keys, target}` (mutación síncrona, sin escritura de archivos) relinkea cada key a la variante de px exacto o `"highest"`, skip `no_variant` (sin grupo o sin esa variante — incluye keys desconocidas) / `already_there` (la actual ya es esa), preserva la forma de la ruta almacenada vía `replace_basename_preserving_form` (relativa/absoluta/`relative:///` — misma lección de 5.2), lote completo en un `StartUndo`/`EndUndo` + `_settle_relink_results` para fallos de escritura por fila. SPA: indicador `⇄` junto al chip Res en filas con variantes detectadas; barrido de variantes (`fetchHubVariants`) corre en su propio efecto, en paralelo al barrido de meta existente; diálogo "Switch res..." (patrón HubShrinkDialog) activo con ≥1 fila seleccionada con variantes, objetivos = unión de px disponibles + "Highest" con contador "X/N disponibles" (`switchTargets` puro en `hubTable.ts`, vitest) → toast de resumen + re-fetch + re-anclaje de stamp + selección limpia. Un Cmd+Z revierte el lote. Verificación (5.3): pytest 664 + vitest 50 + reviews adversariales por tarea.

**Verificación live COMPLETA para 5.2 + 5.3** (C4D 2026.302, MCP + eyeball del usuario, escena real): selección (single/toggle/rango/Escape) OK; shrink 3×4K→2K con progreso, copias `_2K` en disco y un Cmd+Z; copy con colisión probada; conmutador de variantes con las familias reales (`_4k_1`↔`_8k_1` y pares del Shrink) subiendo/bajando resolución. **Siete fixes cazados en vivo y arreglados en la raíz**: (1) shrink/copy relinkeaban con ruta absoluta aunque el original fuera relativo → `replace_basename_preserving_form` conserva la forma (`relative:///`, `tex/`, abs); (2) los pares del Shrink no agrupaban (el original no tiene token) → familias con base sin token, px enriquecido por meta; (3) chips de res con los mismos tints que los estados → escala del acento; (4) Cmd+Z "no funcionaba" tras el diálogo de Switch — el undo del motor era correcto (verificado con CallCommand): la raíz era el FOCO del webview al desmontarse el diálogo (el elemento enfocado desaparece del DOM y el atajo no llega a C4D) → `restoreFocus()` al cerrar cualquier diálogo del Hub; (5) escala de resolución completa 16K→<1K; (6) label del chip = K real más cercano (un HDRI de 6980px decía "4K" con umbrales de piso → ahora "7K" con tier 8k por midpoint — el criterio honesto de Overseer sin su desajuste label/color); (7) escalera de color con tope sólido para que 4K/8K/16K se distingan de un vistazo. Pendiente en deuda: gate inline con `gates_enabled` y fallback nativo forzado sin ejercitar en vivo (code paths revisados); Windows (hardware); `⇄` en filas no-repathable da error en vez de skip (follow-up anotado).
- **v1.0.0** (Oct 2025): Initial release — 5 QC checks, presets, scene tools, snapshot system
- **v1.0.1** (Oct 2025): ABC Retime integration, ACES tone mapping fix
- **v1.0.2** (Nov 2025): Fixed absolute path detection for node materials
- **v1.0.3** (Feb 2026): Create Hierarchy button, removed path popup warning
- **v1.0.4→v1.3.x**: Foundation fixes, UI upgrade, 5 new QC checks, auto-fix, QC report, RS AOV system
- **v1.4.0** (Apr 2026): Take QC, Scene Collector, Light Groups AOV, UI reorganized by workflow
- **v1.4.1** (Apr 2026): QC #11 — FPS/Frame Range validation (start=1001 VFX standard, frame step, timeline+preview alignment, all presets, configurable FPS, playhead snap)
- **v1.4.2** (May 2026): UI polish — score header, click-row in QC; Smart Save Version (versioned `_v###` files with comment + QC score + scene stats + sidecar history JSON)
- **v1.4.3** (May 2026): Smart Save UX — review status tags (WIP/TR/CR/FINAL/custom) baked into filename; "Continue from this version" auto-WIP after review saves; "Last version" live caption above Save Version
- **v1.4.4** (May 2026): Browse Recent Versions inline (custom GeUserArea with status badges + filter dropdown + click-to-open); fixed user-area click coord conversion in C4D 2026 (Local2Global() + msg subtraction) — affects both StatusArea and HistoryArea
- **v1.5.0** (May 2026): **Rebrand YS Guardian → Sentinel** — plugin file renamed (`sentinel_panel.pyp`), settings file renamed (`sentinel_settings.json` with auto-migration from legacy), C4D plugin folder `Sentinel/`, GitHub URLs updated, attribution to Yambo Studio explicit in README/CLAUDE.md/License
- **v1.5.1** (May 2026): Scene Notes & TODOs (sidecar JSON, modal editor, panel caption); Collect Scene clean delivery naming (strips `_v###[_status]` from filename, preserves traceability in manifest); notes integrated in QC report export and Scene Collector manifest + sidecar copied to delivery
- **v1.5.2** (May 2026): UI/UX redesign — Scene Header always visible (filename caption + Shot/Artist + QC progress bar) + 4 tabs (QC / Render / Versions / Tools) using QuickTab CustomGUI + dynamic rebuild via `LayoutFlushGroup` (HideElement does not collapse layout space in C4D 2026, hence rebuild on tab switch). Footer (GitHub / Report Bug) always visible. Documented C4D auto-shrink limitation in known limitations.
- **v1.5.4** (May 2026): **Multi-Format Render Setup** — Render tab → "Generate Format Takes..." opens a modal that creates child Takes for the 5 standard delivery aspects (16:9 / 9:16 / 1:1 / 4:5 / 21:9). Each Take gets a cloned Render Data with format-specific resolution + output path (subfolder or suffix mode). Optional auto-FOV keeps vertical FOV constant across formats via `take.FindOrAddOverrideParam(td, cam, fov_id, target_fov)` (idempotent, updates on re-run). Full undo wrapping. Summary dialog with created/updated/skipped/errors counts. Backed by orchestrator `generate_multiformat_takes(doc, options)` and `MultiFormatDialog` (modal). Math: `target_h_fov = 2*atan((target_aspect/source_aspect)*tan(source_h_fov/2))`.
- **v1.6.0** (Jul 2026): **Motor QC 2.0 — declarative registry + per-project rules + baseline, via incremental modularization**. The monolithic `.pyp` (11,067 lines) becomes a 115-line bootstrap; the engine and UI move into the `sentinel/` package (see Core Files). Three new user-facing capabilities, each shipped as a phase verified against a frozen v1.5.7 oracle: **(1) Declarative check registry** — the 12 checks are entries in `qc/registry.py` (`CHECK_REGISTRY`: id, label, severity FAIL/WARN, fix capability, params); panel, QC Report, Save Version summary and Collector preflight all iterate the registry, so check #13 costs one entry, not N edits. Severity is display-only in v1 (FAIL/WARN formalized but weighted equally in the score — weighting is deferred to the gates work). **(2) Per-project rules** (`sentinel_rules.json`, `rules.py`) — FPS, start frame, approved presets, default names, safe-area insets, per-check severity and on/off, discovered from the scene folder + up to 3 ancestors (nearest wins, no cross-file merge; shadowed files shown in the header and QC Report). Precedence: project rules > machine settings > embedded defaults; per-key validation (a bad `fps: "twenty"` is rejected with a named warning, the rest of the file still applies); the header shows the active ruleset name; resolution is cached by (path, mtime) and invalidated alongside the QC cache so editing/deleting the file takes effect without restart. A disabled check dims its row and drops out of the denominator (`X/11 · 1 disabled`), and the artifacts list disabled checks. **(3) Baseline of accepted violations** (`baseline.py`, `<base>_baseline.json`, schema v1) — accept known violations with mandatory author + reason; the score then counts only **new** violations (a mixed row reads `N new (M accepted)`). Identity = `check_id` + hierarchical path + sibling index + object `GetGUID()` (deleting an accepted `Cube[0]` never lets sibling `Cube[1]` inherit the acceptance; renaming re-arms — assumed RuboCop-style weakness); parametric checks snapshot the rule value and re-arm if the ruleset changes; QC #12 uses `check_id + object + format`, never frames. Merge-on-write (re-read, union by identity, atomic tmp+rename), Synology conflicted-copy detection + merge on load, read-only lockout on an unreadable sidecar (never overwritten with empty). Accept/retire invalidates the QC cache explicitly (an acceptance doesn't dirty the scene). History entries become schema v2 `{passed, total, new, accepted}`; pre-v1.6.0 entries render as `8/12 (legacy)` so the series doesn't read as improvement when only the metric changed. Scene Collector copies and renames the baseline sidecar and the effective `sentinel_rules.json` into the delivery, and the manifest lists acceptances + ruleset origin. **Verification ladder** (audit-mandated, precedes the first extraction): pytest 70/70 on pure helpers + rules/baseline/registry engines, plus a frozen dual oracle (v1.5.7 text byte-identical across the whole arc + a structured oracle) run in **C4D 2026.3** after each unit; deterministic fixtures (`tests/fixtures/violating.c4d` trips all 12 checks, `clean.c4d` passes 12/12) via `tests/c4d_runner/run_fixtures.py`. Reload policy: **restart C4D** — no `sys.modules` purge (purging split-brains live ObjectData/overlay instances). The installer now copies the whole `sentinel_panel.pyp` + `sentinel/` + `res/` folder, not a single file.
- **v1.7.0** (Jul 2026): **Quality Gates (I3)** — QC severity (FAIL/WARN, per-check in `sentinel_rules.json`) becomes actionable at Smart Save Version and Scene Collector. A `GateTriageDialog` lists failing checks with severity and offers: auto-fix the batchable ones (lights→group, camera shift→reset, unused mats→delete, FPS/range→standard) in a **single undo step**, accept into the baseline, or proceed anyway. Fallback dialog rows are tagged with their `check_id`. Gated behind a `gates_enabled` flag (off by default). Core in `gate.py`, wired into Save + Collect. Merged via PR #2.
- **v1.8.0** (Jul 2026): **Sentinel Frame — per-camera multi-format tag with WYSIWYG crop**. One `SentinelFrameTag` (TagData, id 2099073) is the single entry point for the multi-format workflow, subsuming the modal dialog + overlay toggle + mark button. Live viewport guides/mask(opacity)/platform-zones/HUD via `Draw` — **corrects the false v1.5.6 limitation**: `TagData.Draw` DOES fire with `TAG_VISIBLE|TAG_EXPRESSION|TAG_IMPLEMENTS_DRAW_FUNCTION` (flag 256), so no ObjectData companion drawer is needed. Buttons (Create/Update, Set Output, Remove Stale, Mark Subject) call the additive engine: camera-scoped Take naming, host-camera binding, **rename-safe** re-run via BaseLink resolver, single-undo, idempotent Set Output, staleness hash + "Takes out of date" HUD. **Crop-first (`framing.format_crop_values`)**: true inscribed crop via **focal** (universal — works on standard AND Redshift; aperture doesn't, RS ignores it — see [[reference_redshift_camera_params]]), gate-relative film-offset nudge; wider/equal formats crop by resolution alone; guides suppressed while viewing a format Take. QC #12 made nudge-aware (reads the tag's per-format nudge; identical without a tag). Panel: "Add Sentinel Frame to camera"; legacy Multi-Format dialog + Safe-Area Overlay retired from the panel (overlay `ObjectData` fully unregistered, `overlay.py` deleted; `MultiFormatDialog` kept in code so old Takes still work). Verification: pytest 129/129 + live MCP in C4D 2026.301 (standard + Redshift, all formats, render-verified). Merged via PR #3.
- **v1.9.0** (Jul 2026): **Post-Render Validation (I1) — the render safety net**. A "Validate Render Output..." button (Render tab → Post-Render) audits rendered frames **on disk** against what the scene says should exist — closing the gap where Sentinel protected everything up to the render button and nothing after. Pure engine in `plugin/sentinel/postrender.py` (stdlib-only, no `import c4d`; C4D reads are function-local in a thin adapter): sequence gaps, 0-byte/truncated frames, size-outlier SPC (MAD, WARN), previous-session "stale" cluster detection, AOV presence (WARN), per-Take/format coverage. Scene-aware **expected manifest** reads render data + takes (render-selection gate via `IsChecked`/current take — Main correctly excluded when not the render target), range-by-mode (Manual/Current/All), resolution/format→ext, and RS AOVs via extended `aovs.get_rs_aovs` (`effective_path`/`file_format`/`direct_enabled`) + `get_aov_multipart`. Atomic JSON report + a **separate** `<base>_render_history.json` sidecar (never the Versions-tab history — KTD7). Built via the Codex-implements / we-review loop (grounding→brief→adversarial critique→Codex→pytest+mutation+adversarial review→live-MCP). **Confirmed U1 contract** (C4D 2026.301): `c4d.modules.tokensystem.StringConvertTokens`; RS per-AOV `REDSHIFT_AOV_FILE_EFFECTIVE_PATH` (see [[reference_c4d_render_output_naming]]). The first real production run caught two false-positives pytest+review missed and now fixed: beauty↔AOV **prefix collision** (77 false size-outliers — same folder, shared base → anchored `<prefix><sep?><digits>` parser + frame-token detection) and `detect_stale_cluster` **sub-second jitter** (false stale → `MIN_STALE_GAP_SECONDS=300` absolute floor). Verification: pytest 171/171 + mutation + two adversarial passes + live-MCP on the real RS scene (beauty clean, 0 outliers, real gaps flagged).
- **v1.10.0** (Jul 2026): **Collect Confiable (I4) — la entrega con prueba**. Tras el `SaveProject()`, el Collector reabre el paquete colectado en un doc temporal, re-escanea sus dependencias sobre esa copia (el paso donde Save-with-Assets miente) y sella una sección de assets en el `sentinel_manifest.json` existente: estado por asset (`collected`/`missing`/`external`) con procedencia (material/luz/canal), inventario de plugins requeridos (objetos+tags+materiales, IDs ≥1M con denylist de nativos Maxon — XPresso/SDS/Cloner/Data Tag/Bevel verificados en vivo), `scan_status` siempre presente (nunca un manifiesto silenciosamente vacío) y rutas normalizadas a `/` para verify cross-platform. Recepción: botón condicional «Delivery Summary...» (pestaña Versions, solo con manifiesto con `assets_schema` junto a la escena) muestra QC sellado + acceptances + TODOs + plugins, y «Verify» re-comprueba el paquete en el disco del receptor (detección LOST-in-transfer). Motor puro `manifest.py` (stdlib, sin `import c4d`, 24 tests). Verificación: pytest 305/305 + reviews adversariales por tarea + revisión final de rama (1 Critical + 2 Important corregidos) + fixtures en vivo + **entrega real de producción** (4 missing corroborados 1:1 por SaveProject, 0 falsos positivos; LOST con 3 texturas borradas). Dos bugs cazados en producción el mismo día y documentados en `docs/solutions/` (re-scan de entrega stale con rename rehusado; ruido de IDs nativos). Botón de recepción requiere cambio de pestaña (rebuild por tab). PR #13.
- **v1.17.0** (Jul 2026): **UI Fase 5 — Asset Hub en SPA**. El Asset Hub migra a página `?page=hub` (`HubPage`) con inventario virtualizado (`@tanstack/react-virtual`, filas 32px, thumbnails lazy), filtros + búsqueda, y repathing con **pending model** (Find/Replace espejado en TS con test de paridad vitest contra el motor Python, Make All Relative y Search Folder resueltos server-side vía ops nuevas `hub/make_relative`/`hub/match_folder` que reutilizan el motor nativo — no lógica duplicada) y Apply All en un solo undo. Deliver vive en la misma página: franja pre-flight QC (Fix con el confirm contract del Command Palette, Details → Reports QC), gate inline reutilizando `GateChecks` + op `form/gate` con el mismo contrato servidor `gate_blocked`/`gate_ack` de Fase 4, y collect como **job con progreso en vivo**: `JobRegistry` en `webbridge.py` expone `hub/job_status` respondido en el hilo del servidor HTTP (bypass deliberado de la `MainThreadQueue`) para que el polling del cliente no se bloquee mientras el collect ocupa el main thread de C4D; summary inline reutiliza `DeliverySummaryView`. Infra nueva: endpoint binario `GET /thumb?key=` con caché en disco (`prefs/sentinel_thumbs`), `FormDialog` extendido con `query` (`?page=hub&focus=deliver`) para deep-link al foco de entrega, `pump_jobs()` añadido a los `Timer` de ambos hosts (Reports y Forms), y polling de `hub/state_stamp` con **re-anclaje desde stamps de mutación** — equivalente stateless del `_suppress_ticks` nativo (evita que el propio polling pise una mutación local antes de que el snapshot del servidor la refleje). Refactor de deuda: helper compartido `gate.build_preflight_issues` retira el loop de preflight que estaba duplicado entre `dialogs.py` y `flows.py`. Entradas migradas con fallback nativo si el servidor falla: Tools → Asset Hub, botón Collect (abre con `focus=deliver`), QC #6 Assets Info, y comando `open_hub` del Command Palette; `AssetHubDialog` se conserva en código como fallback (patrón `MultiFormatDialog`/`TextureRepathingDialog`). Verificación: pytest 551 + 12 reviews adversariales por tarea. **Fase 5.1 — inspector de texturas** (mismo release, encadenada sobre Fase 5): el Hub gana metadatos de imagen por fila sin decodificar píxeles — motor puro nuevo `imagemeta.py` (parsers de cabecera PNG/JPEG/TIFF/EXR/HDR/TGA/BMP vía `struct`, nunca importa `c4d`, nunca lanza — corrupto/desconocido → `None`), `vram_bytes(w,h,ch,depth)` con factor de mipmaps ×4/3 y defaults defensivos, y `res_bucket(max_px)` como única fuente de la etiqueta+color de resolución (8K/4K/2K/<2K) — ni la SPA ni ningún otro punto re-derivan el bucket. Ops nuevas en `hub_ops.py`: `hub/meta` (lotes ≤64 claves, caché por `(path, mtime, size)` igual que el patrón de thumbs) y `hub/meta_totals` (solo caché, nunca parsea) — `hub/inventory` añade los totales VRAM desde esa caché sin coste extra. `hub/ui_state` persiste anchos de columna + orden en `GlobalSettings` bajo la clave `hub_spa_ui` (patrón `texture_repath_presets`). SPA: filas pasan a dos líneas (44px fijo, la virtualización no cambia) — nombre + chip de resolución + badge de estado arriba, ruta·dimensiones·canales·profundidad·colorspace·primer owner abajo; barrido de metadatos asíncrono tras el inventario (chunks secuenciales de 64 claves, se re-arma automáticamente si el usuario aborta a mitad de escaneo); columnas redimensionables por arrastre (persistidas, saneadas al cargar, doble-click en el divisor restaura el ancho por defecto); cabeceras de columna clicables para ordenar (ciclo asc→desc→por defecto = missing primero, luego peso descendente); facetas Res/Canales/Profundidad con contadores que componen sobre el filtro de estado + búsqueda existentes. Estudiado contra Overseer (estudiar-sí/copiar-no): caché+lazy en vez de escaneo bloqueante, virtualización en vez de paginación, bucket de una sola fuente en vez de doble cómputo cliente/servidor, parsers de cabecera en vez de Pillow. Fase 5.2 (backlog, no en este release): Shrink in-place + copy-into-project; "Accept missing" descartado en brainstorming — su equivalente ya existe como baseline QC. Verificación de Fase 5.1: pytest 601 + 5 reviews adversariales por tarea (fixes en vivo: aritmética VRAM del mock, re-arme del sweep tras abort, saneo de `ui_state` cargado). **Verificación live COMPLETA (C4D 2026.302, MCP + eyeball del usuario, escena real de producción)**: inventario 39 assets con paridad 1:1 contra el Hub nativo; preflight 6/12 = panel; thumbs E2E (generación 64px → caché disco → HTTP 200 `image/png`); collect job con progreso VIVO (polls respondidos durante save/rescan/zip con el main thread ocupado — el bypass de `hub/job_status` probado en producción; zip 590MB, 4 missing sellados = histórico v1.10); metadatos contrastados contra Overseer como referencia cruzada (mismos archivos → mismas dims/canales/depth/VRAM). **Cinco bugs cazados en vivo y arreglados en la raíz**: (1) el op de thumbs re-escaneaba la escena entera por petición (jank de scroll + polling congestionado) → memo `_THUMB_PATHS` alimentado por cada scan; (2) el stamp de polling era ciego a los repaths (el cambio de path de textura no toca el dirty del documento) → 5º componente = suma de `GetDirty(DATA)` de materiales, medido en vivo; (3) escenas sin guardar duplicaban assets (el resolver no buscaba en el tex/ de usuario ni en `GetGlobalTexturePaths()` donde C4D sí resuelve) + fila fantasma del propio doc → resolver con dirs globales inyectables + exclusión de self-reference; (4) el sweep de metadatos podía quedarse incompleto para siempre tras un abort a mitad (stamp prematuro) → stamp solo al completar; (5) los resizers de columna iban al revés — geometría real: con la columna 1fr a la izquierda, el límite solo sigue al puntero si el divisor vive en el borde izquierdo y redimensiona la columna derecha con delta invertido. Pendiente en deuda: ejercitar en vivo el gate inline con `gates_enabled` y el fallback nativo forzado (code paths revisados); verificación Windows (hardware).
- **v1.16.0** (Jul 2026): **UI Fase 4 — formularios en SPA + toasts + Command Palette**. Save Version, Scene Notes, Settings y Gate Triage pasan a páginas HTML con el design system, servidas en `FormDialog` (host hermano de Reports, tamaño por formulario, mismo servidor/cola). Requisito previo resuelto en el motor: **cancelación de peticiones** en `MainThreadQueue` (lock por request) — una petición que el cliente dio por muerta nunca se ejecuta tarde, que es lo que hace seguras las ops de **mutación**. Las ops de submit reutilizan los flujos nativos exactos (`smart_save_version`, `notes.py`, `GlobalSettings`, `gate.py`/`apply_fixes`) — cero lógica duplicada — y los diálogos nativos quedan como fallback ante cualquier fallo del servidor. **Toasts** (success/info/warn, 4s) sustituyen los popups de resultado de estos flujos. **Command Palette** (⌘K-style): busca y ejecuta cualquier acción del plugin — navegación, formularios y quick-fixes — con **confirmación inline obligatoria** para las dos acciones destructivas (borrar materiales sin uso, reescribir FPS/rangos), gate impuesto en el contrato del servidor (`requires_confirm` + `confirm_required`), no solo en la UI. Entrada por menú Help y por comando propio al que asignar atajo. El gate de calidad sigue siendo modal nativo dentro de Save/Collect (el flujo es síncrono y no se rediseña); `form/gate` es triage independiente desde el palette. Verificación: pytest 517 + 5 reviews adversariales (3 hallazgos corregidos: acciones destructivas sin confirmar, colisión de widget ID 1316, ruta `form/save_version`) + **2 bugs cazados en vivo por el usuario** y arreglados en la raíz: la página del palette en blanco (payload `{"actions": [...]}` sin desenvolver → guard defensivo) y los diálogos abiertos por atajo/palette recolectados por el GC (registros de retención en `open_form`/`open_reports`).
- **v1.15.0** (Jul 2026): **UI Fase 3 — consolidación de arquitectura de información nativa**. Una sola respuesta a "¿dónde vive X?": **Snapshots** — el panel muestra el directorio efectivo con su origen (`auto-detected from RenderView` / `manual fallback`), el Browse del panel se elimina y el campo de Settings se deshabilita con nota cuando la detección funciona (patrón FPS/ruleset); de paso, bug real cazado: Save Still ignoraba la auto-detección (`_find_latest_exr` solo miraba el setting manual) — unificado vía `get_effective_snapshot_dir()` con tests de regresión. **Multi-Part EXR** — auditado: el split ya era correcto (default de estudio en Settings, estado de escena en Render), sin cambios. **Pestaña Versions → Deliver** (Save Version + Recent + Notes + Collect/Supervisor como puerta única de entrega). **Menú Help** (GitHub, Report Bug) — footer reducido a Settings + Doctor. Fase 6 candidata (panel completo como SPA) anotada en ROADMAP pendiente de brainstorm. Verificación: pytest 456 + review adversarial + eyeball del usuario en C4D 2026.302.
- **v1.14.0** (Jul 2026): **UI Fase 2 — Reports completo + triage de popups**. Las cuatro superficies de informe restantes migran a Sentinel Reports con el design system: **QC Report** (12 checks con severidad, `N new (M accepted)`, detalles expandibles, ruleset chip — mismo scoring que el panel vía los helpers compartidos, cero lógica duplicada), **Doctor**, **Supervisor** (input de carpeta + Scan, tabla de shots por sidecars) y **Render Validation** (lee el último informe del validate; empty state si no hay). Deep-link `?page=` para que cada botón nativo abra su página (Doctor/Supervisor/Open QC Report/Validate → Reports, con el diálogo legacy como fallback ante cualquier excepción). **Triage de popups**: inventario completo de los 108 MessageDialog/QuestionDialog en `docs/superpowers/specs/2026-07-19-popup-triage.md` — 12 informativos convertidos a `safe_print` + barra de estado (verificando superficie inline real caso a caso), 71 decisiones intactas, 12 diferidos a la fase de toasts. Hallazgo: `collect_scene` confirmado sin llamadores (retirado de facto). Verificación: pytest 453 + 4 reviews adversariales + live con datos reales (QC 6/12 de la escena de producción coincidiendo con el panel, Supervisor encontrando 2 shots reales, popups en status bar) + eyeball del usuario.
- **v1.13.0** (Jul 2026): **UI Foundation (Fase 1 del rediseño) — Sentinel Reports con HTML embebido**. Primer entregable del rediseño UI/UX (spec `docs/superpowers/specs/2026-07-18-ui-redesign-design.md`, brainstorm con companion visual): design system **Linear-adaptado** formalizado en `docs/design/DESIGN.md` (formato getdesign.md; tokens exactos con equivalencias `c4d.Vector` para el sabor nativo; regla: el acento `#5e6ad2` nunca marca estado — la croma semántica es exclusiva de fail/warn/pass/neutral). Infraestructura: `webbridge.py` puro (cola main-thread con invariante read-only, servidor estático+API con contención realpath, 29 tests) + `ReportsDialog` (`CUSTOMGUI_HTMLVIEWER` — hallazgo clave: el gadget es **WebKit moderno** en C4D 2026 y el Cmd+Z atraviesa al documento, spike verificado) + SPA Vite+React+TS+Tailwind v4 en `web/` con build committeado en `plugin/web/` (node solo en dev). Primera página: Delivery Summary leyendo el manifest real del paquete activo (mapper puro `delivery_report_payload` verificado campo a campo contra manifest.py y los tipos TS). Arquitectura estudiada de Overseer (licencia estudiar-sí/copiar-no; implementación propia, endurecida: sin CORS, realpath, port-fallback). Fases 2-5 (Reports completo, consolidación IA nativa, formularios+⌘K, Hub) pendientes en ROADMAP. Verificación: pytest 433 + build reproducible bit-a-bit + Playwright + live en C4D con la entrega real de producción (39 assets, API round-trip servidor→cola→main-thread→JSON) + eyeball del usuario.
- **v1.12.0** (Jul 2026): **Snapshot Watchfolder — auto-convert opt-in**. Toggle en el panel (off por defecto, persistido en `sentinel_settings.json`) que vigila la carpeta de snapshots de Redshift desde el `Timer` del panel (throttle 2,5s). Regla de settle basada en scans (no wall-clock): un EXR solo se convierte cuando dos scans consecutivos ven (mtime, size) idénticos — nunca archivos a medio escribir; al activar, el backlog existente se marca como procesado (solo convierten los snapshots NUEVOS). Conversión silenciosa (`snapshot_auto_convert`: sin diálogos ni Picture Viewer, nada bloquea el Timer) reutilizando el pipeline ACES EXR→PNG hacia la carpeta de stills del artista, con caption informativo en el panel. Alerta no-EXR: si el archivo más nuevo del watchfolder no es `.exr`, el caption avisa de que RenderView perdió el ajuste "Save snapshots as EXR" (estado Qt de sesión, limitación documentada). Motor puro `scan_snapshot_candidates` en `snapshots.py` (registry en memoria, importable sin c4d). **Flujo streamlined** (mismo release): (1) auto-detección del directorio de snapshots leyendo `snapshotDir` de `prefs/redshift_rv.cfg` (`parse_rv_snapshot_dir` puro + `detect_rv_snapshot_dir` adapter; el detectado gana sobre el picker manual, que queda de fallback; RenderView escribe el cfg al salir → follow best-effort, cacheado por prime); (2) los snapshots no-EXR (png/jpg/tif) también se procesan — passthrough `copy2` sin ACES (ya vienen display-referred), con caption informativo en vez de alerta bloqueante; (3) nombres únicos `<escena>_snap_###` con contador compartido entre extensiones (`next_snapshot_name` puro) — las ráfagas ya no se sobrescriben. Verificación: 25 tests pytest + live en C4D 2026.302 con EXR real en dos fases + PNG (settle correcto, detección ignorando un decoy manual, `_snap_001.png` ACES + `_snap_002.png` copia, captions OK, settings restaurados). También en v1.12.0: pulido UI del Asset Hub (feat/hub-polish, 10 commits): filtros QuickTab horizontales, cabecera bicolor a dos lados, franja pre-flight con fondo de color, badges read-only, columnas redimensionables con divisores visibles y anchos persistidos (fit-to-viewport), botón nativo "Relink Selected..." (patrón Crate, sustituye al glifo por fila), supresión del rescan auto-provocado al seleccionar, y **clave canónica de dedupe** (`canonical_asset_key` + `base_dir`) que fusiona el mismo asset reportado en formas distintas por los dos scanners (`file://`, `relative://`, `./D:/...`) — verificado en escena real: 43 → 39 filas, cero duplicados.
- **v1.11.0** (Jul 2026): **Asset Hub — inventario unificado + entrega en un solo diálogo**. Unifica **Texture Repathing** (v1.5.7) y **Scene Collector** (v1.10.0) en `AssetHubDialog`: inventario completo (scan de texturas estructurado + `GetAllAssetsNew`, archivo propio de la escena excluido), estado por asset, tamaño + totales, procedencia clicable (used-by → selecciona material/objeto en escena), thumbnails lazy cacheados por `Timer`. Decisiones cerradas en brainstorming: **tabla plana** (sin grupos colapsables) con columna Type + filtros; missing al collectar = **avisar y permitir continuar** (estilo Crate, el manifest los sella igual); salida **Folder + Zip opcional** (`zipfile` stdlib, la carpeta de origen se conserva). Repathing multi-shader: un Find/Replace o un Search Folder for Missing reescribe TODOS los shaders que comparten una ruta en un solo `StartUndo`/`EndUndo`. Pre-flight QC embebido reutiliza `run_all_checks`/`apply_fixes`/`BaselineActionDialog` sin cadenas de MessageDialog. Motor puro nuevo `assets.py` (stdlib, sin `import c4d`) — merge/dedupe, clasificación de estado y tipo, totales, matching de missing con detección de ambigüedad. Collect refactorizado a `run_collect_pipeline` (parametrizado, callback de estado, zip opcional) — camino legacy byte-equivalente, manifest sellado v1.10 intacto. Tres puntos de entrada migrados: Tools → "Asset Hub..." (antes "Texture Repathing..."), botón Collect Scene del panel, y QC #6 Assets Info. `TextureRepathingDialog` se retira de la UI pero la clase se conserva una versión (patrón `MultiFormatDialog`). Verificación: pytest 355/355 (~50 tests nuevos sobre `assets.py`) + reviews adversariales por tarea + live MCP en C4D 2026.302 + dos fixes cazados en vivo: el inventario incluía el propio archivo de la escena como asset, y 5 ajustes de UI de captura (tabla top-aligned, columnas recortadas, anchos de label, caption de tamaño, mensaje de "sin coincidencias" más claro).
- **v1.5.7** (May 2026): **Texture Repathing tool**. New multi-renderer bulk find/replace + smart-fix utility for texture paths — built for both project supervision (whole-scene asset validation) and artists (one-button fixes, undo-safe). Tools tab → "Asset Management" → "Texture Repathing...", plus a contextual launch from QC #6 Assets Info. Backbone: `scan_all_texture_paths(doc)` returns structured TextureRecords across every storage mechanism found by empirical probing — RS/Arnold maxon node graphs (`GetChildren()` walk, `GetPortValue()` leaves), classic Xbitmap shader chains, Octane legacy image shaders (`ID_OCTANE_IMAGE_TEXTURE` 1029508), material/object BaseContainer filename params, RS Dome Light HDR (`obj[ROOT_ID, REDSHIFT_FILE_PATH]` compound DescID), Alembic caches, and tag shader chains (Octane Environment Tag, Arnold Sky). Writers in `apply_texture_path_change` dispatch per source_type; node-graph writes use a maxon transaction with **mandatory** explicit `transaction.Commit()` (the `with` exit rolls back silently otherwise). UI: scrollable `TextureListArea` UserArea wrapped in a native `ScrollGroup`, status filter dropdown, Bulk Find/Replace (case-insensitive by default + "Match case" checkbox; `re.sub` with a lambda replacement so Windows backslash paths survive), last-5 Find/Replace presets persisted in `sentinel_settings.json`, Smart Actions (Auto-Find Missing, Make All Relative, Clear pending), per-row `[...]` file picker. **Undo**: Apply All wraps the batch in `StartUndo`/`EndUndo`; for node graphs `doc.AddUndo(c4d.UNDOTYPE_CHANGE, mat)` is the anchor that lets the transaction's `UndoMode.ADD` join the document undo step — a single Cmd+Z reverts the whole batch (verified: `Edit → Undo Modify <mat>`). Dialog is **async** not modal (a modal dialog captures the keyboard, so Cmd+Z never reached C4D); `CoreMessage`/`Timer` auto-refresh keeps the list in sync after an external undo. Diagnostic note: `doc.DoUndo()` called from the Script Manager is unreliable and is NOT a valid proxy for real Cmd+Z — verify undo via the Edit menu instead. V-Ray support intentionally dropped (out of studio scope).
- **v1.5.6** (May 2026): **Safe-Area Viewport Overlay**. Closes the v1.5.5 deferred work. After confirming `c4d.plugins.SceneHookData` is gone in C4D 2026 AND `TagData.Draw` is registered but never invoked (Init+Execute fire, Draw doesn't), pivoted to `ObjectData.Draw` in `DRAWPASS_OBJECT` which works reliably. Architecture: `SafeAreaOverlayObject(plugins.ObjectData)` auto-created at scene root when the Render-tab toggle is enabled. Marker is identified by plugin TYPE (`SAFE_AREA_OVERLAY_PLUGIN_ID = 2099072`), not name. Draw queries `bd.GetSafeFrame()` for the in-viewport letterboxed render rectangle, maps each format's master-NDC safe-box to pixel coords, draws 4 outline lines + HUD label per format. Per-format colors (white master, orange Reels, cyan square, magenta portrait, yellow cinema). Module-level `_overlay_state` singleton shared between panel and marker. New `plugin/res/` folder with `description/safearea_overlay.res` (`INCLUDE Obase`), `.h`, and `.str` for the localized name. Multi-Format orchestrator now also refreshes the overlay cache after Take regeneration. Composition Mode interaction documented: overlay always uses crop-interpretation model (matches QC #12), so Mode None + post-crop matches exactly; Mode Resize Canvas overlay is a composition reference (each take recomposes the camera per format).
- **v1.5.5** (May 2026): **Cross-Aspect Safe-Area QC (#12) + Multi-Format refactor**. New QC check verifies opt-in marked subjects (UserData boolean `[Sentinel] Safe Area Subject`) stay inside per-format safe areas across all active Multi-Format delivery Takes. Uses crop-interpretation math: bbox projects ONCE into master NDC, each format's safe area lives there as a centered crop region with per-side insets. Sample strategies "current_frame" (auto-refresh, cheap) and "keyframes" (Info button, full sweep with `SetTime`+`ExecutePasses` per sample). Tools tab → smart-toggle "Mark / Unmark Safe Area Subject" button. Score header now `X/12`. Multi-Format refactor: replaced Auto-FOV checkbox with Composition Mode dropdown (None / Resize Canvas). Resize Canvas uses sensor-size override (`new_aperture = src × target_w / src_w`) matching AR_ResizeCanvas community script. v1.5.4 carry-over fixes: `take.SetCamera` now assigned; `SetParameter` explicit after `FindOrAddOverrideParam` (defends against find-OR-add silent skip); C4D physical/RS cameras clamp FOV overrides — switched to sensor (aperture) override which isn't clamped. Other fixes: panel `_refresh` crash on non-QC tab reopen; NDC projection corrected from `-Z forward` to `+Z forward` (C4D left-handed convention). **Deferred to v1.5.6**: live viewport overlay (`c4d.plugins.SceneHookData` removed in C4D 2026 — pending TagData / MessageData investigation).

## Testing Checklist
- [ ] Main plugin file loads without errors
- [ ] All 12 quality checks function correctly
- [ ] Select/Fix/Info buttons work per check
- [ ] Auto-fix: lights→group, cameras→reset shift, unused mats→delete, FPS/range→standard
- [ ] FPS/range fix preserves duration, aligns timeline + preview, snaps playhead
- [ ] FPS/range fix shows confirmation dialog with diff preview
- [ ] FPS/range fix iterates ALL presets (not just active)
- [ ] Stills preset accepts Current Frame mode; animation presets require Manual range starting at 1001
- [ ] Save Version: doc unsaved → SaveDialog → file saved as `<base>_v001.c4d`, doc renamed
- [ ] Save Version: subsequent calls bump version, history JSON appended (newest first)
- [ ] Save Version: empty comment is rejected, Cancel is no-op
- [ ] Save Version: title bar updates after save, future Cmd+S overwrites latest version
- [ ] Status tags: WIP/TR/CR/FINAL applied as `_TR`, `_CR`, `_FINAL` filename suffixes (custom alphanumeric supported)
- [ ] Status tags: version bump correct across mixed status (`v002_TR` → `v003_REV02` → `v004`)
- [ ] "Continue from this version" prompt only after TR/CR/FINAL; auto-creates WIP version on Yes
- [ ] "Last version" pillbox updates on save and shows relative time ("just now" → "Xm/h/d ago")
- [ ] "final" in comment triggers soft warning (advisory dialog, doesn't block)
- [ ] Recent Versions list shows last 5 entries with color-coded status badges (WIP/TR/CR/FINAL)
- [ ] Filter dropdown (All/WIP/TR/CR/FINAL) updates the list correctly (WIP shows only `""` status entries)
- [ ] Click on a version row → confirmation dialog + opens that .c4d file via LoadFile
- [ ] Click row edge cases: file deleted → "File not found", same-as-active → "Already viewing", unsaved changes → warning
- [ ] Scene Notes: edit dialog opens with hint about shared-across-versions scope
- [ ] Scene Notes: notes + TODOs persist across Cmd+S and Save Version
- [ ] Scene Notes: panel caption shows ⚠ prefix when there are pending TODOs
- [ ] Scene Notes: TODOs toggle/delete/add work in dialog, Cancel discards changes
- [ ] Collect Scene: collected `.c4d` is renamed to clean original base (no `_v###` suffix)
- [ ] Collect Scene: manifest contains `original_filename`, `original_version`, `original_status`
- [ ] Collect Scene: notes sidecar copied with matching base name, panel reads it correctly
- [ ] Collect Scene: success dialog warns when there are pending TODOs
- [ ] RS AOV Essentials + Production tiers apply correctly
- [ ] Light Groups: diagnose + toggle on Beauty
- [ ] Scene Collector: pre-flight + SaveProject + manifest
- [ ] Take QC: validates camera and output tokens
- [ ] QC Report export produces valid JSON
- [ ] Render Presets: dropdown, resolution label, Reset All, Force 9:16
- [ ] Multi-Format: "Generate Format Takes..." opens modal seeded from current take/resolution
- [ ] Multi-Format: 5 child takes created under source take (16x9, 9x16, 1x1, 4x5, 21x9)
- [ ] Multi-Format: each take's render data has correct resolution + output path (subfolder or suffix)
- [ ] Multi-Format: Composition Mode "None" → camera intact across formats; vertical extent of master subject changes per format aspect (default C4D behavior)
- [ ] Multi-Format: Composition Mode "Resize Canvas" → CAMERAOBJECT_APERTURE overridden per format via `new_aperture = src × target_w / src_w`; angular field "rotates" between formats
- [ ] Multi-Format: re-run with "Update existing" ON → existing takes updated; stale FOV/focal overrides from prior runs reset to native
- [ ] Multi-Format: re-run with "Update existing" OFF → existing takes go to Skipped list
- [ ] Multi-Format: single Cmd+Z reverts the entire batch (StartUndo/EndUndo wrap)
- [ ] Multi-Format: every generated Take has source camera assigned via `take.SetCamera(td, cam)`
- [ ] QC #12: row appears in QC tab with Select + Info buttons; score header reads X/12
- [ ] QC #12: with no marked objects → row always reads `[ OK ]`
- [ ] QC #12: with no Multi-Format Takes → row always reads `[ OK ]`
- [ ] QC #12: cube marked + filling master → row reports violations (auto-refresh = current frame)
- [ ] QC #12: Info button → full keyframe sweep dialog with per-object, per-format, per-frames + edges breakdown
- [ ] QC #12: Select button → selects all marked objects with at least one violation (deduplicated)
- [ ] QC #12: after Info sweep, current timeline frame is restored (no scrub leakage)
- [ ] QC #12: keyframed object samples include union of PSR-track keyframes + midpoints
- [ ] QC #12: crop interpretation — same cube might fit in 16:9 master but violate 9:16 horizontal crop (and vice versa for 21:9 vertical)
- [ ] QC #12: asymmetric insets respected (9:16 violates bottom 15% before top 8%)
- [ ] Tools: "Mark / Unmark Safe Area Subject" button smart-toggles selection (all marked → unmark; any unmarked → mark all; empty → hint dialog)
- [ ] Tools: Mark operation is single-undoable via Cmd+Z
- [ ] Safe-Area Overlay: Render tab checkbox toggles overlay on/off
- [ ] Safe-Area Overlay: toggle ON auto-creates "Sentinel Safe-Area Overlay" object at scene root
- [ ] Safe-Area Overlay: 5 colored rectangles visible in viewport (white/orange/cyan/magenta/yellow per format) with fmt_id labels
- [ ] Safe-Area Overlay: deleting the marker object + toggle ON recreates it
- [ ] Safe-Area Overlay: regenerating Multi-Format Takes auto-refreshes the cached rectangles
- [ ] Safe-Area Overlay: rectangles correctly positioned inside `bd.GetSafeFrame()` (handles letterbox/pillarbox)
- [ ] Texture Repathing: Tools tab → Asset Management → "Texture Repathing..." opens the async dialog
- [ ] Texture Repathing: QC #6 Assets Info → offers to launch the tool when there are path issues
- [ ] Texture Repathing: scan lists RS/Arnold node textures, classic shaders, Octane image shaders, RS Dome HDR
- [ ] Texture Repathing: status filter (All/Missing/Absolute/OK/Asset URI) updates the list + scrollbar
- [ ] Texture Repathing: list scrolls (ScrollGroup) when there are more rows than fit
- [ ] Texture Repathing: Find/Replace is case-insensitive by default; "Match case" toggle enforces exact case
- [ ] Texture Repathing: Preview shows pending changes in green; Apply All commits + shows summary
- [ ] Texture Repathing: Apply All on a node-graph texture is reverted by a single Cmd+Z (Edit → Undo Modify <mat>)
- [ ] Texture Repathing: Make All Relative / Auto-Find Missing / per-row [...] file picker work
- [ ] Texture Repathing: last-5 Find/Replace presets persist in sentinel_settings.json, recallable via Recent combo
- [ ] Texture Repathing: list auto-refreshes after an external Cmd+Z (CoreMessage/Timer)
- [ ] Scene Tools: all 8 buttons functional (Hierarchy, H→Layers, Solo, Drop, Vibrate, ABC Retime, Cam Simple, Cam Shakel)
- [ ] Output: Open Folder, Save Still, Export QC, Collect Scene
- [ ] Snapshot dir picker ("...") persists between sessions
- [ ] Artist name persistence works
- [ ] Shot ID syncs with Take system
- [ ] Cross-platform: macOS and Windows

### Current UI Layout (v1.8.0):
```
┌─ Sentinel v1.8.0 ──────────────────────────────┐
│  Scene Header (always visible) ─────────────   │
│  ▸ Scene:  test_v007_TR.c4d                    │  ← filename caption
│  Shot ID: [Main]   Artist: [Motioneer]         │  ← editable
│  QC 9/12  ⚠  ████░░░░  ·  1.2M polys           │  ← score line
│ ─────────────────────────────────────────────  │
│  ┌───┬────────┬──────────┬───────┐             │
│  │QC●│ Render │ Versions │ Tools │ ← QuickTab  │
│  └───┴────────┴──────────┴───────┘             │
│ ─────────────────────────────────────────────  │
│  (active tab content — dynamically rebuilt     │
│   via LayoutFlushGroup on switch; only one     │
│   tab is in the layout at a time because       │
│   HideElement does not collapse layout space   │
│   in C4D 2026)                                 │
│                                                │
│  Tabs:                                         │
│    QC: 12 quality check rows + Export QC       │
│        (#12 = Cross-Aspect Safe Area)          │
│    Render: Preset + Multi-Format + AOVs + Snap │
│    Versions: Notes + Save Version + Recent     │
│    Tools: Layout / Animation / QC Marking /    │
│           Asset Management (Asset Hub...)      │
│                                                │
│ ─────────────────────────────────────────────  │
│  Footer (always visible)                       │
│  [GitHub]                  [Report Bug]        │
└────────────────────────────────────────────────┘
```
